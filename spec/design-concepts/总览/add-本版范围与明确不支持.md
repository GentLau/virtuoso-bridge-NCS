# 本版范围与明确不支持

> 版本：v9
> 日期：2026-09-15
> Supersedes：v8（业务接口计数口径：5 业务接口 + 只读查询 query）
> 状态：Normative（“本版不做什么”的唯一口径）
> 说明：本文是正式范围文档；README 中的清单只索引本文。每项给出本版口径，实现按此处理，不得静默降级。

## 1. 本版支持的范围（一句话）

四层架构：上层通过中层**五个业务接口**（Skill 执行 / 命令执行 / 文件执行 / GUI 命令执行 / Spectre 命令执行）完成业务，另有一个只读查询 `query`（见[四层整体架构与接口 §4.2](1-四层整体架构与接口.md)）供参数准备；中层负责 local/SSH/多用户路由与投送；底层 daemon 只执行 Skill。多用户、并发、CDS.log 增量返回在本版范围内。以下明确不做。

## 2. 明确不支持清单（唯一口径）

### 2.1 上层业务封装

| # | 不支持 | 本版口径 |
|---|---|---|
| 1 | Spectre 高层业务封装（仿真流程、结果解析、编排） | 只提供 **Spectre 命令执行**（一次性命令）与 `role.spectre.bin` 记录，不提供 `purpose`、不做仿真编排；封装留给上层后续；探测/校验失败只 warning、不阻断注册 |
| 2 | GUI 图形化业务封装 / 独立 deploy role | 只提供 **GUI 命令执行**（一次性命令，窗口枚举/dismiss/bootstrap 类操作）；部署只投送到 `role.daemon.root`，不设独立 deploy role |

### 2.2 旧协议与兼容

| # | 不支持 | 本版口径 |
|---|---|---|
| 3 | 无 token 旧客户端 / 旧 il | 请求缺失、null 或不匹配 token → NAK `invalid token`，SKILL 零接触 |
| 4 | `profile` / `VB_*` / `.env` 及迁移适配 | 已删除、无兼容入口；新用户只走六步注册 |
| 5 | 非对称签名 / HMAC | token 是对称共享授权票，不做双方公私钥；四层边界不因未来升级而变（见[四层架构 §5.8](1-四层整体架构与接口.md)） |
| 6 | `request_id` | 不引入请求字段；daemon 同一时刻只有一个 in-flight，增量按 `[start,end)` 定界（见[日志返回设计标准 §3](../底层/6-日志返回设计标准.md)） |
| 7 | argv / 无 shell 模式、异步 command handle | RunCommand 为同步 shell 字符串；长任务由上层 marker/轮询实现 |

### 2.3 日志

| # | 不支持 | 本版口径 |
|---|---|---|
| 8 | split-host CDS.log | 不保证：daemon 能读到 IL 回传的 `path` 就按普通路径返回增量；读不到 → `log=""` + 固定 warning；`log_level=off` 时源头不读、无 warning；bridge 不做 split-host 判定（见[日志返回设计标准 §2](../底层/6-日志返回设计标准.md)） |
| 9 | CDS.log 增量跟随 GUI 读取 | 增量读取固定锚定 **daemon**（读 IL 回传的 `path + [start,end)`），不在 GUI 主机读 CDS.log（X11 转发场景同样不做）；实现点见[日志返回设计标准 §2](../底层/6-日志返回设计标准.md) |
| 10 | 全量日志 parser / push stream / 日志数据库 | 只做同步增量返回；全文经 File 接口自行读取 |

### 2.4 文件

| # | 不支持 | 本版口径 |
|---|---|---|
| 11 | 文件空间安全沙箱 | `role.<name>.root` 只是各 role 的默认工作目录约定，不拦截 `..`/任意绝对路径；隔离依赖 OS 账号权限 |

### 2.5 daemon 生命周期

| # | 不支持 | 本版口径 |
|---|---|---|
| 12 | 跨机启动 daemon 的协议（mpsserver / 远程 launcher 等） | 本版不实现；当前 daemon 是 CIW 的本地 `ipcBeginProcess` 子进程，启动方式与运行账号由站点拓扑决定，bridge 只忠实投送与连接、不参与也不校验 |

## 3. 与相关文档的关系

- 每项对应的“该怎么做”由各自 Normative 文档定义：接口见[四层整体架构与接口](1-四层整体架构与接口.md)，路由见[路由设计](../中层/3-路由设计.md)，注册见[多用户与注册](../其他/1-多用户与注册.md)，并发见[并发设计](../中层/2-并发设计.md)，日志见[日志返回设计标准](../底层/6-日志返回设计标准.md)，字段见[中层配置文档](../中层/add-中层配置文档.md)。
- 路由专项的“不做”项（role 间 mode 一致性约束、主机/账号/路径一致性校验、跨主机文件可见性、GUI/Spectre 常驻会话、业务接口总数固定为 5）见[路由设计 §5](../中层/3-路由设计.md)，本文不复制。
- 本文只回答“本版不做”，不重复具体机制。