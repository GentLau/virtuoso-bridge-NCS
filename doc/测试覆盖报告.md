# 测试覆盖报告

> 日期：**2026-09-15（最新快照）**；历史快照 2026-09-11
> 覆盖对象：`src/`（pyapi / transport / bridge / server）
> 覆盖方式：coverage.py 语句覆盖，`--source=src`；Windows 与 wsl-gent 两份数据按“执行行并集”合并
> 结论（2026-09-15）：**离线三层语句覆盖率 75%**（5003 语句 / 1264 未覆盖，
> `coverage run --source=src -m pytest test/unit test/integration test/scenario`）；
> 真机证据已补齐，见[五接口三环境真机测试报告](report/五接口三环境真机测试报告.md)（S1–S15）与
> [反思与缺陷记录](report/反思与缺陷记录.md)。本报告不宣称 100%。

---

## 0. 2026-09-15 更新摘要

- **覆盖率**：75%（5003/1264）；分模块：registry 96% / register.flow 88% / registration_server 87% /
  reservation 84% / middle 88% / stress_server 81% / tunnel 75% / ssh 57% / paramiko_backend 59% / daemon_3 68% / daemon_27 69%。
- **新增用例**：五接口错误契约 13 项（`test_middle_contracts.py`）、stress_server 端点 4 项、
  隧道生命周期 4 项（BUG-1/BUG-3 回归）、runner 单飞 2 项（BUG-2 回归）、资源释放 2 项、
  注册页契约 6 项、注册 HTTP 层 6 项（per-role 覆盖 / local 校验 / 端口作用域 / 重复 token / 端口冲突）、
  错误路径与诊断 14 项（`test_transport_error_paths.py`）、paramiko 助手 10 项、常驻 shell 协议循环 8 项、
  本地 `~` 展开 2 项。
- **修复的缺陷**（详见反思文档）：BUG-1 隧道重复 spawn（进程风暴）、BUG-2 runner 竞态、
  BUG-3 退出不拆隧道（孤儿进程）。三者均有回归测试与真机资源计数证据。
- **真机证据**：S1–S15（单用户五接口 / T2 跨主机 / T3 混合 / 六步注册+压力 / CDS.log 契约与逐字节 /
  win-mixed / wsl-mixed / 16 用户混合 / 修复后 T2·T3 复跑）。
- **测试纪律新增**：压测从强客户端（glis/wsl-gent）发起、vps ≤40 并发；每轮做**客户端侧**资源盘点
  （ssh 进程数 ≈ 用户数、无孤儿隧道）；TB 统一隐藏 Windows 控制台。

## 1. 测试体系与规模

```text
test/
  unit/         256 项（1 项平台性 skip）  函数级：纯函数、模型、解析、mock 分支、stress HTTP 门面
  integration/   14 项                   集成级：真实 socket 假 daemon、daemon 子进程、wire 协议
  scenario/      14 项                   场景级：六步注册、多用户隔离、完整业务模拟（skill/command/file/并发）
  e2e/           10 项（按环境分档）      真实场景级：单用户远端全流程 + 57 用户远端业务并发 + 6 用户本地业务并发
test_bak/        旧 tests/ 归档，不参与常规运行
```

- 离线（Windows，无 Virtuoso）：`unit 256 + integration 14 + scenario 14 = 284` 项，全部通过。
- 真机：远端 `wsl-gent` 单用户 e2e + **57 个真实 Virtuoso 的远端业务/并发**；本地把代码部署到 `wsl-gent`，注册 **6 个本地账号**跑本地业务/并发，全部通过。
- 离线三层（unit 267 + integration 14 + scenario 15 = 296 项）全部通过，语句覆盖率 **73%**。

### 各层覆盖的责任边界

| 层 | 覆盖内容 | 主要文件 |
|---|---|---|
| 函数级 | 传输计划、路径模型、协议解析、注册模型/查重/探测 mock、SSH 选项与重试、Paramiko 配置解析 | `test/unit/test_transfer.py`、`test_ssh.py`、`test_paramiko.py`、`test_register_flow.py`、`test_daemon_parity.py` 等 |
| 集成级 | STX/NAK/RS 与 JSON 请求的真实 socket 往返、token 校验零接触、超时、临时 il 打包、daemon 子进程启动/端口占用 | `test/integration/test_daemon_handler.py`、`test_integration.py` |
| 场景级 | 本地六步注册（apply→load→verify→commit→业务三接口）与双用户隔离 | `test/scenario/*` |
| 真实级·远端 | wsl-gent 真实 Virtuoso：注册探测→部署→CIW load→连通→Skill 含 log→命令→文件 sha→并行区间重叠；Paramiko 真机命令+文本上传 | `test/e2e/test_e2e_live.py` |
| 真实级·远端并发 | 57 个真实 daemon：`RBDToken` 逐用户路由校验、三接口混合并发、拒绝即重试至全部成功 | `test/e2e/test_business_remote_live.py` |
| 真实级·本地并发 | wsl-gent 本地模式：六步注册本地账号→每账号一个真实 Virtuoso→三接口业务+并发 | `test/e2e/test_business_local_live.py` |

---

## 2. 覆盖数字（双环境执行行并集）

总计（本快照，离线三层）：**4505 语句，1231 未覆盖，73%**。

| 模块 | 语句 | 覆盖 | 说明 |
|---|---|---|---|
| `pyapi/models.py` | 50 | 98% | 返回模型与接口定义 |
| `server/registration_server.py` | 173 | 84% | 注册 HTTP 页面与 API |
| `server/stress_server.py` | 84 | 100%* | HTTP 压力门面（新增单测覆盖） |
| `transport/registry.py` | 151 | 97% | 注册表校验/原子落盘/token 索引 |
| `transport/register/flow.py` | 359 | 92% | 六步注册编排 |
| `transport/register/models.py` | 88 | 98% | 注册申请模型 |
| `transport/register/probe.py` | 154 | 84% | 远程/本地探测 |
| `transport/middle.py` | 156 | 96% | 中层三接口门面 |
| `transport/skill_client.py` | 79 | 99% | Skill TCP 客户端 |
| `transport/tunnel.py` | 130 | 96% | 每 token 远端连接 |
| `transport/deploy.py` | 67 | 85% | 共享部署（无路由） |
| `transport/transfer.py` | 152 | 98% | tar/text 传输计划与原子安装 |
| `transport/ssh.py` | 934 | 69% | OpenSSH 执行/传输/持久 shell/隧道重试 |
| `transport/paramiko_backend.py` | 775 | 63% | Paramiko 会话复用后端 |
| `bridge/…/ramic_bridge_daemon_3.py` | 316 | 71% | 底层 daemon（Python 3） |
| `bridge/…/ramic_bridge_daemon_27.py` | 311 | 71% | 底层 daemon（Python 2.7 兼容） |
| 其余小模块（__init__/paths/setup 等） | — | 79–100% | 路径/生成器/导出 |

> *`stress_server.py` 经 `test/unit/test_stress_server.py` 覆盖 skill/command/upload/download/404/缺 token 全分支。

> 未对 `ramic_bridge.il` 做语句覆盖率统计（SKILL 语言，coverage.py 不适用），只做静态断言 + 真机验收，见 §5.4。

---

## 3. 关键业务场景覆盖（对应 spec）

### 3.1 六步注册（多用户设计 §3.2）

- 申请参数校验：远程模式 host/ssh_user 必填、端口 1–65535、token 字符集、user 非空；
- 第二步本地校验：user 查重 + daemon/local 端口查重（单测 + 页面 400 路径）；
- 第三步探测：只做 daemon 可用前即可验证的项目——SSH 可达、host key 指纹、hostname、daemon 用户存在性、python 选型、端口空闲/分配（避让注册表已占端口）、部署根可写；**用户未填以探测固化，显式填写的参数做可用性校验**（如 local_port 占用、spectre 路径不可执行都会报告参数问题）；每个失败分支都有单测且不落盘；
- 第四步部署：python 2/3 选 `_27/_3`，setup IL 注入 token 并**每次 load 打印 token**（新增 `printf("[RAMIC] token=%L\n" RBDToken)`，有单测 + 真机 load 验证）；
- 第五步连通：daemon 可用后补齐第三步无法完成的全部校验——命令冒烟 + Skill 冒烟 + token 比对 + **host key 指纹运行时比对（不匹配 ERROR）** + banner 主机名漂移 WARNING，失败重试不落盘；
- 第六步唯一写盘：`verify()` 成功才 `registry.register()`；**verify 幂等**（重复调用保持 committed）；注册表写盘先 `tmp+replace` 成功再提交内存，失败回滚。

### 3.2 token 机制

- 三接口 token 均为必填 keyword-only，漏传 `TypeError`（四个接口全覆盖）；
- 未知 token 结构化失败且不构造远端连接；
- daemon 无 token 启动即退出；请求 token 缺失/null/不匹配 → `15 invalid token 1e`，且**不向 Virtuoso 管道写入任何字节**（假管道断言 `written == b""`）；
- 单行/多行 SKILL 帧中不含 token 明文（断言 sent 字节）。

### 3.3 注册表

- `user` 为键、token 唯一、`mode ∈ {local,remote}`、端口/线程池/channel/超时/log 配置全部带约束校验；
- `load()` 一次进内存；运行时零探测、零写盘；
- 原子替换、落盘失败内存回滚、`.json.tmp` 不残留；
- 模型 dump 不包含 `status/authorized` 等运行态字段。

### 3.4 CDS.log 增量返回

- token 使用范围：路由/校验用 token；**用户可读路径用唯一用户名**（`~/.virtuoso-bridge/<user>/`），路径层有单测 + 真机验证；
- 两个 daemon 用**同一组 fixture 做 parity**：`off/all/warn/error` 分级、未知前缀按 info、超长降级 error-only、error-only 头尾 100 行、UTF-8 按字节计数；
- `_read_delta` cursor：增量推进、路径切换重置、文件轮转从 0 重读、`end<start`/`end>size` 夹紧（本次修复了负长度读取崩溃）；
- 真机 E2E 断言 `execute_skill` 返回非空 `log`。

### 3.5 并行机制与并发

- 本地与真机均验证：`parallel=True` 两条命令的**时间区间重叠**（E2E 用远程时间戳区间，不依赖易抖动的墙钟阈值）；
- 线程池超限、channel 预算超限均返回结构化错误，客户端**立即重试直至成功**。
- **100 用户自动路由压力（协议级假 daemon）**：混合 600 命令 + 400 Skill + 200 上传 + 200 下载，全部 200、路由错配 0、sha 不匹配 0、无崩溃断连。

### 3.7 完整业务模拟：远端模式 57 真机 + 本地模式 6 真机

- **远端并发**（`test/e2e/test_business_remote_live.py`，Windows 驱动）：57 个真实 Virtuoso 各注册独立 token/端口；`RBDToken` 逐用户路由校验 57/57 一致；三接口混合并发（skill 456 次 + command 456 次 + 上传/下载各 114 次），拒绝即重试，最终**全成功、路由错配 0、sha 不匹配 0**。
- **本地并发**（`test/e2e/test_business_local_live.py`，wsl-gent 上运行）：代码部署到 `/home/Gent/vb-ncs`，6 个本地账号走完整六步注册（注册表唯一落盘点），每账号一个真实 Virtuoso（`.cdsinit` load 生成的 setup）；业务三接口 + 并发混合全部通过，**全程无 SSH**（中层与 Virtuoso 同机直连）。
- 本地/远端共用同一套 `BusinessServer` 三接口，切换仅在注册表 `mode` 与路由字段，上层无感知。

### 3.6 文件传输

- tar/text 上传计划、目录下载计划、staging、原子安装、失败回滚旧文件（覆盖目录替换/文件替换/异常回滚）；
- 上传/下载**对称**：`recursive=False` 单文件 + sha256 校验，`recursive=True` 目录 tar（本地 `copytree`/`copy2` 镜像），目录误用/单文件误用均有错误分支；
- 真机 OpenSSH 上传/下载往返校验；Paramiko 上传文本 + `cat` 校验；
- 业务模拟并发中上传/下载各数百次，sha256 零不一致。

---

## 4. 双 subagent 审查 → 已落地修复

两个只读审查代理给出了缺口清单与可疑逻辑。本轮已落地的修复：

1. `middle.py` 线程池信号量在 `_acquire→_remote` 之间被覆盖导致 `Semaphore released too many times`（真实并发记账 bug）；
2. `skill_client.py` `socket.timeout`（OSError 子类）先于 `except OSError` 捕获，原先超时被误报为 Socket error；
3. `ssh.py test_connection` 依赖 ControlMaster，Windows 陈旧 mux 使注册探测误判不可达 → 改为无 CM 探测；
4. `ssh.py` Windows 隧道 stderr 文件句柄父进程未关闭 → 已关闭；
5. `probe.host_key_fingerprint` 只支持 DNS 直连 `ssh-keyscan` → 改为 known_hosts 优先 + `ssh -G` 别名解析 + keyscan 回退；
6. `daemon_27.py` 混用 `chr()+bytes` 在 Python 3 下无法执行 → 统一 bytes 帧（保留 Python 2.7 兼容）；
7. `daemon_3.py` 的 3.7+ 注解语法在 Cadence 自带 Python 3.6 上 SyntaxError → 全部改为 3.6 兼容写法；
8. `_read_delta` 恶意/异常 meta 可触发负长度读取或 cursor 越过 EOF → 已夹紧 `[0,size]`；
9. `registry.register/remove` 原先先改内存后写盘，落盘失败产生分叉 → 改为先持久化成功再提交内存；
10. 注册/注册表字段约束缺失（mode 枚举、端口范围、token 字符集、log_level 枚举、正数约束）→ 已在模型层统一校验；
11. `setup.il` 未打印 token，目视确认授权流程缺失 → 已加入每次 load 打印；
12. `verify()` 重复调用会把 committed 翻成 failed → 已幂等。

---

## 5. 未覆盖的 22%：构成与原因（不隐瞒）

### 5.1 `transport/ssh.py`（69%）

未覆盖主要是：
- **POSIX 专属持久 shell 生命周期细节**（`ensure_persistent_shell` 的旧进程重建、kill/wait 超时、reader join 边界）：开发机是 Windows，persistent shell 在 Windows 上按策略禁用，走不了真实路径；已用 mock 覆盖协议状态机主体；
- **tar 管道双进程编排的完整失败矩阵**（stdout/stderr drain 线程、communicate 超时清理）：plan 与安装层已覆盖，进程编排部分依赖真机，真机只验证了成功路径；
- **Windows 专属 `start_port_forward` 失败分支的 settle 30s/10s 下限与临时文件删除**：只覆盖成功路径。

### 5.2 `transport/paramiko_backend.py`（63%）

未覆盖主要是 connect 链路的深层编排：
- `_connect_client`/`ensure_connected` 的跳板 `direct-tcpip`、SOCKS 握手、host-key-alias socket、失败清理顺序——需要 mock 或真实跳板/SOCKS 拓扑；
- SFTP/tar 的真实慢速传输、callback 超时是否被 Paramiko 包装；
- 真机只覆盖 `run_command` + `upload_text` 两条。

### 5.3 底层 daemon（71%/71%）

- `start_server` 的 accept 主循环、watchdog 对 Virtuoso 的 SIGINT、banner/identity 的真实 IPC 行为只能由真实 CIW 覆盖（本地覆盖率进程无法统计远端子进程）；已用 handler 级假管道覆盖请求处理主体；
- `_read_frame` 的 POSIX `EAGAIN` 分支在 Windows 上不可达。

### 5.4 SKILL（`ramic_bridge.il`）

coverage.py 无法统计 SKILL。用静态断言 + 真机验收：BEGIN→flush→执行→END→flush→path/offset 第二帧的顺序、`ipcBeginProcess` 注入 token、load 打印 token、identity 文件格式。

### 5.5 平台差异

- `runtime_paths` 的 macOS/Linux XDG 默认目录分支：Windows 开发机无法构造 POSIX 路径对象，标为 platform skip；
- daemon Python 2.7 的真实解释器行为：现场有 Cadence 自带 python2.7，但本机只有 Python 3；`daemon_27` 已做双版本 bytes 兼容修复并用 py3 近似执行，真正的 2.7 现场执行列为待验。

### 5.6 当前明确声明的功能/验收缺口（scope 边界，非静默缺口）

1. **split-host 独立探测**：手动注册单主机完整；`daemon_host ≠ command host` 时对 daemon 主机单独探测与路径可见性检查未实现；
2. **split-host CDS.log**：本版日志增量仅支持 daemon 与 CDS.log 同主机（三层整体架构设计 §5.2 已声明，split-host 日志为后续版本）；
3. **Spectre**：不自动探测 `spectre.bin`（本版只显式提供 + 可用性校验）；`RuntimePurpose`/purpose 路由与 Spectre 垂直切片未实现（spec/README 已声明为后续扩展）；
4. **现场压力/拓扑**：SOCKS5、跳板真实拓扑、MaxSessions 临界并发、断线自愈的压力测试未执行；
5. **真实 Python 2.7**：`daemon_27.py` 只做了 Python 3 近似执行与语法兼容修复，尚未在 Cadence 自带 python2.7 现场实测；
6. **clean-checkout 安装验收**：`pyproject`/CI 已修正，但尚未在干净环境执行 `uv build` + 安装 wheel 后跑 CLI/resource smoke（现场网络条件限制）。

以上均为已声明的边界或后续版本工作，不属于“测试报告失实”。

---

## 6. 如何复现

```bash
# 离线三层（无需 Virtuoso）
coverage erase
coverage run --source=src -m unittest discover -s test/unit -p "test_*.py"
coverage run -a --source=src -m unittest discover -s test/integration -p "test_*.py"
coverage run -a --source=src -m unittest discover -s test/scenario -p "test_*.py"
coverage report --skip-empty

# 真实场景·远端（Windows；需要 wsl-gent 上的 bridge daemon）
$env:VB_E2E='1'
coverage run -a --source=src -m unittest discover -s test/e2e -p "test_*.py"

# 真实场景·本地（在 wsl-gent 上；先把 src/test 部署过去）
scp -r src test wsl-gent:/home/Gent/vb-ncs/
ssh wsl-gent 'cd /home/Gent/vb-ncs && python3 -m venv .venv && .venv/bin/pip install pydantic paramiko pytest coverage'
ssh wsl-gent 'cd /home/Gent/vb-ncs && VB_E2E_LOCAL=1 VB_LOCAL_USERS=6 \
  .venv/bin/python -m unittest discover -s test/e2e -p "test_business_local_live.py" -v'
```

---

## 7. 结论

- 四层测试体系离线 284 项 + 真机 10 项全绿；双环境合并语句覆盖 **78%**；
- 业务三接口（skill/command/file）在**远端 57 个真实 Virtuoso**与**本地 6 个真实 Virtuoso**下完成混合并发验证，路由零错配、文件零不一致、被拒即重试直至成功；
- 未覆盖部分绝大多数是“只能在跳板/SOCKS 真实拓扑、Python 2.7 现场解释器、POSIX persistent-shell 细节中触发的路径”，已在 §5 逐条说明，**不把 78% 当作 100%**；
- 距离更高覆盖率的路径明确：跳板/SOCKS 真机夹具、现场 python2.7 跑 `daemon_27`、§5.6 功能缺口补完后回归。
