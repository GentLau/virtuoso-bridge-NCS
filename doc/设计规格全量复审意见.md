# Spec 全量复审意见（2026-09-21，最终范围，排除上层 2–10）

> 复审日期：2026-09-21
> 评审对象：`C:\Users\user\Desktop\repos_Github\virtuoso-bridge-NCS\spec\`
> 评审快照：HEAD `288ebcb036562b7cb7a5fcc8a792462d12b0d082`
> Release：`SPEC-2026-09-21-r6`
> Normative 文件集：10 份
> Normative hash：`3c5a49f3c6bc26a0466f0463160179234930a47d6c5f74824f1cbcbf7e10c0c2`
> `spec\protocol-v1.md`：不存在，未纳入评审。

## 评审范围

按此前口径，`spec\design-concepts\上层\2-*.md` 至 `10-*.md` 全部不在本轮评审范围内。本轮评审对象是 README 冻结的 10 份 Normative 文档：

1. 四层整体架构与接口；
2. 本版范围与明确不支持；
3. 中层配置文档；
4. 路由设计；
5. 多用户与注册；
6. 并发设计；
7. 日志返回设计标准；
8. 顶层：HTTP 入口与业务调度；
9. 顶层补充：控制面与业务面；
10. 上层：业务包与插件化（仅 `1-上层.md`）。

## 一、结论

> **审批结论：通过。当前评审范围内未发现剩余的基本行为冲突、完整机制重复或缺失 owner 索引，可以签署最终冻结。**

上一轮遗留的 `/api/bug` 个人 token 校验职责已经收口：

- 顶层总则将 token 不解析、不读 registry 等禁令限定到业务请求处理链路；
- 控制面将 `/api/bug` 标注为控制端口权限校验例外，并明确由控制进程经注册与管理模块的 registry 内存快照校验，不经中层、不读注册表文件；
- bug 报告落盘前剥离 token/Authorization 等凭据。

同时，状态转移表已经补齐 validate/probe/deploy/verify/commit 的失败原地重试条件；Release 已升级为 `SPEC-2026-09-21-r6`。

## 二、静态治理复核

`python tools/check_spec.py` 全部通过：

| 检查项 | 结果 |
|---|---:|
| Normative hash | `3c5a49f3c6bc26a0…`，通过 |
| Manifest 版本 | 10/10 匹配 |
| 相对 Markdown 链接 | 302 个通过 |
| JSON fenced block | 17 个通过 |
| owner/banned-copy | 0 hits |
| 数字标题重复 | 0 |

排除上层 2–10 后的范围内补充检查：

- 范围内相对链接 283 个，缺失 0；
- 范围内 JSON 17 个，解析失败 0；
- 范围内数字标题重复 0。

## 三、本轮关闭的问题

- 顶层业务请求链路 token 合法性由中层判定，控制端口 `/api/bug` 的校验例外已明确；
- 顶层不读 registry 的禁令已限定为业务请求处理链路，控制面的注册与管理模块校验来源已明确；
- 顶层进程准入上限与中层 per-token 预算已分离；
- 注册失败候选保留、原地重试、cancel/服务重启释放的规则已在多用户、配置和控制面三方一致；
- 管理面的子进程与内部通道不再被顶层业务请求链路禁令否定；
- bug 原始请求体凭据脱敏、16 MiB 上限和失败不记录已明确；
- 顶层任务等待池保持本版不支持，没有残留旧设计；
- Release、Manifest、hash 和 Supersedes 已同步。

## 四、不作为问题的实现自由度

- 业务进程管理内部通道的具体实现；
- `/api/process/restart` 的同步等待超时具体数值；
- bug 报告附带的近期日志选择策略；
- 16 MiB 请求体的读取和拒绝实现；
- 控制面注册与管理模块的内存快照结构；
- 上层 2–10 的业务包细节。

## 五、最终意见

> **批准当前 10 份 Normative Spec，允许作为设计冻结基线。**

上层 2–10 明确不在本次评审范围内，不随本意见获得结论。
