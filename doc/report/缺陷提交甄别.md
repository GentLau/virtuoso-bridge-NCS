# 缺陷提交甄别（2026-09-20）

> 数据来源：`log/bug_reports/*.json`（经 `/api/bug` 提交，共 12 份：
> 11 份缺陷/观察条目 + 1 份我方大小探针测试记录）
> 角色：中层/底层/顶层代码负责人对提交内容的甄别与处置
> 纪律：先判断是否真实、是否在本层职责内，再决定修复 / 驳回 / 转上层；修复一律 TB 先行

## 一、判定汇总

| # | 提交内容（摘要） | 判定 | 处置 |
|---|---|---|---|
| B1 | [P1] 自动分配端口不查注册表：新用户 probe 拿到与已提交用户相同的 `daemon_port`/`local_port`，直到 commit final re-check 才失败 | **真实（我层）** | 已修：自动分配把已提交注册表端口纳入预留（daemon_port 按目标主机、local_port 按本机）；TB `test_auto_allocated_ports_avoid_committed_registry_ports` |
| B2 | [P1] commit 失败后留在 step=5，同一步原样重试被 400 `step order violation` | **真实（我层）** | 已修：commit 失败前先置 `step=6`；TB `test_commit_failure_keeps_step6_and_can_retry` |
| B3 | [P2] 常驻 shell 执行 `exit N` 返回假超时（124/timeout）而非真实 rc | **真实（我层）** | 已修：回包改走预存的原始 fd（9/8），EXIT trap 在命令重定向仍生效时也能回传真实 rc；非退出路径先解除 trap 再显式 emit，常驻 shell 的 cwd/env 保持。TB `test_script_reports_real_rc_even_if_command_exits_shell` + 真实 POSIX shell 端到端 TB `test_exit_returns_real_rc_in_a_real_posix_shell`、`test_command_state_persists_across_calls_in_real_shell`；真机复验见 §三 |
| B4 | [P2] 控制面畸形 `Content-Length` → 无任何 HTTP 响应（连接被断） | **真实（我层）** | 已修：非法/负值显式报错，由调用方返回结构化 4xx；不能退化成“空 body 走默认参数”，否则 `/api/process/reload` 会被畸形头触发真实 reload。TB `test_malformed_content_length_returns_4xx`、`test_malformed_content_length_is_400_and_does_not_reload` |
| B5 | [P2] 非法 `Content-Length` 在 `/api/bug`、业务面返回 400，控制面其它端点不返回 | **真实（我层）** | 同 B4（B4 是其根因）；修复后 register/update/config/process 全部走统一 4xx；同时补上 process 端点畸形 JSON 与非 object body 不得按默认 target 执行（TB `test_malformed_json_is_400_and_does_not_reload`，覆盖 `{bad` 与 `[]`） |
| B6 | [P2] `demo.paths.facts` 不校验个人 token，未知 token 也返回 200 + 本机路径 | **真实，但属上层** | 不在本层范围：该 operation 完全不访问中层，token 合法性按 spec 由中层判定；转上层（建议先 `middle.query(token=...)` 或移除该 op） |
| B7 | [P2] GUI 操作是"显示级"的：同一 X display 上任何用户都能枚举/操作别人的 Virtuoso 窗口 | **驳回（非 bridge 缺陷）** | `路由设计 §1` 忠实投送原则：role 拓扑（含是否共用 display）是用户拓扑责任，bridge 不判断、不校验；多用户共用 display 就会互相可见。需要隔离应由上层（10-gui）或站点拓扑（每人独立 display）解决 |
| B8 | [观察] `/api/bug` 落盘时把 token 掩码，而不是字面"原文" | **不是缺陷（已由 spec 收口）** | 控制面 v28 已改为"先剥离 token/Authorization 等凭据再记录"；实现按 v28 扩展为掩码 token/authorization/password/secret 等；TB `test_bug_report_strips_credentials` |
| B9 | [观察] verify 成功时 `banner_hostname=null` 且无 warning | **驳回（spec 未要求）** | 多用户与注册 §3.2 只规定"不一致 → WARNING"；取不到 banner 属站点拓扑（identity 文件由 IL 写入）表现，未定义即不判定；如需要可作为后续 spec 提议 |
| B10 | [观察] 注册提交后业务进程不会自动感知新 token，需要 reload/restart | **不是缺陷（设计如此）** | v27 已提供 `POST /api/process/reload|restart`（管理员）；运行期不自动读文件是 spec 明确要求 |
| B11 | [已修/归档] `virtuoso.gui.*` OPERATIONS 里用字符串代替 Request 类 | **已是历史问题** | 上层已修复；本层仅确认 build 后 51 个 operation 全部可调度 |
| B12 | [测试探针] `size probe` 大字段记录（token 已掩码） | **非缺陷** | 我方畸形/大小探针产生的测试数据，不进入修复范围；已在本表统计口径中单列 |

## 二、驳回理由详述

### B7（GUI 跨用户可见）：不是 bridge 的隔离缺陷

- 《路由设计》§1"忠实投送原则"：bridge 只按用户给定目标投送，不判断 role 之间是否同机、同账号、共享路径/display；
- 《多用户与注册》§1：token 只防 Skill 通道误连，不承担 SSH 认证；隔离依赖 OS 账号权限；
- 两个用户都把 `gui` role 指向同一台机器的同一个 `DISPLAY=:99`，在该 display 上就是互相可见——这是站点拓扑（"用户拓扑责任"），不是 bridge 越权；
- 若站点要求隔离：每用户独立 X display / 独立账号，或由上层业务包增加"只操作自己的窗口"过滤（属 10-gui 口径）。

### B9（banner=null 无 warning）：spec 未定义的行为

- 《多用户与注册》§3.2 只定义 `banner hostname / daemon user 与 expected_* 不一致 → WARNING`；
- identity 文件缺失/读不到属于站点侧事实；注册第五步已实际连通（skill 冒烟通过）才可能走到这里；
- 需要"取不到 banner 也提示"的话，应在 spec 增加一句，再实现（属可选项，不阻断）。

## 三、修复后的真机复验

（重启 supervisor 后执行，环境：Windows 客户端 → WSL-gent 远端模式）

- 常驻 shell 状态：`pwd` → `[0, "/tmp\n", "", "command"]`；执行
  `echo before-exit; exit 7` → `[7, "before-exit\n", "", "command"]`，
  耗时 0.20s（修复前为假超时/unknown-effect）；随后 `pwd` 返回新 shell
  的 `/home/Gent`，再执行 `echo after-restart-ok` 成功，证明退出后按规范重建；
  加载最终修复后再次复验 `echo live-exit; exit 9` → `[9, "live-exit\n", "", "command"]`，
  随后 `echo post-exit` 成功；
- raw socket `Content-Length: abc` → `HTTP/1.0 400 Bad Request` +
  `{"error":"invalid request: user is required"}`（修复前 0 字节断连）；
- raw socket 打到 `/api/process/reload`（管理员 token）时：
  `Content-Length: abc` → `400 {"error":"invalid Content-Length"}`；
  正确长度但 body=`{bad` → `400 {"error":"invalid JSON body"}`；
  body=`[]` → `400 {"error":"request body must be an object"}`；
  都不会按默认 target 触发真实 reload；最后再以 `exit 11` 复验真实 rc 与
  退出后的会话重建；
- 注册流程 apply→validate→probe（不指定端口）→ 新用户自动得到
  `daemon_port=65082`/`local_port=65082`，避开已提交用户的
  65081、65121、65201、52901；复验后已 `cancel` 释放候选；
- `/api/bug` 提交含 `authorization`/`password` 字段 → 落盘内容已掩码，
  非凭据字段保留，`credentials_stripped=true`；验证后已删除该探针报告。
