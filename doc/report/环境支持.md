# 环境支持文档

> 维护者：Codex（IT 维护）
> 维护范围：三台环境之间的 SSH 连通性
> 最后检查：2026-09-15 12:12 — `vps` ✅ / `wsl-gent` ✅

## 1. 三台环境

| # | 名称 | 角色 | 系统 | 说明 |
|---|---|---|---|---|
| 1 | 本机 | 客户端 / 开发机 | Windows | SSH 命令发起端，Tailscale `100.123.188.27`（DESKTOP-F143LSD） |
| 2 | wsl-gent | Virtuoso 目标机 | Linux（WSL2 · AlmaLinux-8） | 运行于中转机 glis-desktop，地址 172.18.80.17 |
| 3 | 云服务器 | 远端端点 | Linux（Debian） | 公网 114.215.183.227，SSH 别名 `vps` |

## 2. 网络拓扑

```text
本机 ──直连──────────────► vps（114.215.183.227）
本机 ──经 glis-desktop──► wsl-gent（172.18.80.17）
```

glis-desktop（Windows，Tailscale `100.123.135.71`，登录用户 `ljt03`）是 wsl-gent 的中转点。
172.18.80.17 为 WSL 内部地址，本机不能直连。

## 3. SSH 接入

`~/.ssh/config` 相关条目：

- `vps` → 114.215.183.227，用户 `root`，密钥 `id_ed25519_vps`。
- `wsl-gent` → 172.18.80.17:22，用户 `Gent`，密钥 `id_ed25519`，`ProxyJump ljt03@100.123.135.71`。
- `100.123.135.71`（glis-desktop）→ 用户 `ljt03`，默认密钥。

项目 `~/.virtuoso-bridge/.env`：
`VB_REMOTE_HOST=wsl-gent`、`VB_REMOTE_USER=Gent`、`VB_SSH_BACKEND=paramiko`（直接读取上述 config 别名与 ProxyJump）。

## 4. 健康检查

```bash
ssh -o BatchMode=yes -o ConnectTimeout=15 vps echo ok
ssh -o BatchMode=yes -o ConnectTimeout=20 wsl-gent echo ok
```

通过标准：两条命令均输出 `ok`。检查后更新本文档首部的"最后检查"行。

## 5. 已知故障与恢复

**排查顺序**：先验证中转机 `ssh 100.123.135.71`，再验证 `ssh wsl-gent`。
两者都超时 → 5.2；仅 wsl-gent 超时 → 5.1。

### 5.1 WSL 发行版停止（中转机在线）

**症状**：`ssh wsl-gent` 卡在 banner 超时，但 `ssh 100.123.135.71` 可连接。

**原因**：glis-desktop 重启（如 Windows Update 自动重启）后 WSL 发行版 AlmaLinux-8 停在 `Stopped`，172.18.80.17 消失。

**恢复**：
1. `ssh 100.123.135.71 "wsl.exe -l -v"` —— 确认发行版为 `Stopped`；
2. `ssh 100.123.135.71 "wsl.exe -d AlmaLinux-8"` —— 启动 WSL；
3. 重跑第 4 节健康检查。

### 5.2 glis-desktop 整机离线

**症状**：`ssh wsl-gent` 与 `ssh 100.123.135.71` 同时超时；`tailscale status` 显示 `glis-desktop ... offline`。

**原因**：glis-desktop 关机、休眠或断网。本机与其局域网（192.168.1.3）不同网段，无远程路径可达。

**恢复**：需现场开机；开机后 WSL 不会自启，继续按 5.1 处理。

**预防**：关闭 glis-desktop 自动睡眠（或启用 Wake-on-LAN），并配置 WSL 开机自启。