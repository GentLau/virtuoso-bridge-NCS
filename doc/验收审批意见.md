# Spec 验收审批意见（SPEC-2026-09-14-r2·增量复审·2026-09-15）

> 审批日期：2026-09-15
> 当前仓库 HEAD：`a3c0147ea6c659e789e0efcd7f1f265674043d58`
> 最新 Normative Spec 提交：`34b76146605d01dc1230e0334564baa287ca99a8`
> `34b7614..HEAD` 对 `spec/` 无差异；本轮其它文档更新不纳入 Spec 审批。
> Release ID：`SPEC-2026-09-14-r2`
> Normative 内容哈希：`65f0800947137a3290058d692dd595aaeb24bf7c39864a3aa26bc61142a08bee`
> 详细评审：`C:\Users\user\Desktop\repos_Github\virtuoso-bridge-NCS\doc\设计规格全量复审意见.md`
> `spec\protocol-v1.md` 已删除，本轮不纳入评审。
> 范围：**仅审批 Spec；不评价代码、旧代码、测试、测试计划或测试报告。**

## 一、审批结论

> **有条件通过：架构方向通过，最终文字冻结暂缓。**

本轮确认正式 Spec 没有新增/修改内容，因此审批结论与上一轮一致。冻结前需要完成：

1. 统一默认 Paramiko 与 Skill tunnel 的连接数量/复用口径；
2. 选定逐 role 探测的唯一 owner，并冻结 spectre role 的 WARNING 范围；
3. 澄清 SSH alias/`~/.ssh/config` 的 transport 解析边界；
4. 删除“各 role 主机/端口必填”与配置 owner 的矛盾；
5. 把 local daemon 的 `daemon_port/local_port` 作为一个联合 reservation；
6. 修正全局 SSH default host/user 的必填条件；
7. 统一重试次数为“最多 3 次总尝试（含初次）”；
8. 统一 split-host CDS.log 的当前可观察合同；
9. 将探测结果写入候选对象而不是表述为提前写 registry。

## 二、已确认通过

- 五接口、五 role、per-role mode 与跨主机忠实投送；
- 一个 token 一个活动 daemon/CIW；
- root 最终绝对路径的 canonical registry 形态；
- reservation 大流程、stale 回收和等待 load 续租；
- `invalid token` 基础合同；
- File 基本合同与 checksum 结果语义；
- 投递后命令不自动重发；
- endpoint key、channel 记账和 CDS.log frame 的 owner/index；
- 非 22 SSH alias/跳转端口拒绝规则；
- Manifest、版本链、链接、JSON 示例和内容 hash；
- `protocol-v1.md` 已删除并排除。

## 三、重复机制意见

owner/index 已消除大多数无意义重复。仍需重点处理两组：

- **逐 role 探测**：多用户与多节点均保留完整内容，且 owner 互相指向；应一处完整定义，另一处只保留流程摘要；
- **SSH 连接拓扑/生命周期**：架构、配置、多节点、多用户、并发重复描述，当前默认 Paramiko 又使“一条连接”和“业务连接+专用 tunnel”并存。

root、接口映射、File、endpoint key、channel 记账、日志 frame 的必要摘要不是问题，不要求零重复。

## 四、生效条件

完成上述最小收口后：

1. 递增受影响文档版本并更新 `Supersedes`；
2. 更新 README Manifest 与 Normative 内容 hash；
3. 保持 Spec 工作树干净；
4. 重新执行一次 Spec-only 冲突与重复复审。

> **最终意见：批准设计方向；完成最小文本收口后批准最终冻结。**
