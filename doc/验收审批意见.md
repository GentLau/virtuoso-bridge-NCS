# Spec 验收审批意见（2026-09-21，最终范围，排除上层 2–10）

> 审批日期：2026-09-21
> 审批对象：`C:\Users\user\Desktop\repos_Github\virtuoso-bridge-NCS\spec\`
> 评审快照：HEAD `288ebcb036562b7cb7a5fcc8a792462d12b0d082`
> Release：`SPEC-2026-09-21-r6`
> Normative 文件：10 份
> Normative hash：`3c5a49f3c6bc26a0466f0463160179234930a47d6c5f74824f1cbcbf7e10c0c2`
> 范围：只审批 Spec 的冲突、重复和 owner/index；不检查代码与测试。

## 一、审批结论

> **通过。批准当前 10 份 Normative Spec 作为设计冻结基线。**

上一轮 `/api/bug` 校验职责冲突已经闭合，状态转移表也已补齐失败原地重试条件。当前范围内未发现新的基本行为冲突、完整机制重复或 owner/index 缺失。

## 二、本轮重点复核

- 顶层业务请求链路不解析 token、不读 registry、合法性由中层判定；
- 控制端口 `/api/bug` 是明确的权限校验例外，经注册与管理模块 registry 内存快照校验；
- bug 报告落盘前剥离 token/Authorization 等凭据，无效 token 不记录；
- 顶层全局 `business_thread_pool_size` 与中层 per-token 预算职责分离；
- 注册失败保留候选，validate/probe/deploy/verify/commit 可原地重试，参数变更需 cancel 后重新 apply；
- 候选只在 cancel、服务重启或第六步提交成功时释放，多用户与配置文档一致；
- 管理面业务进程监督能力不再与顶层业务请求链路禁令冲突；
- Release ID、日期、Manifest、Supersedes 和 Normative hash 已同步。

## 三、验证结果

`python tools/check_spec.py` 全部通过：

- Normative hash 通过；
- 10 份 Manifest 版本匹配；
- 302 个相对链接通过；
- 17 个 JSON 块通过；
- owner/banned-copy 为 0 hits；
- 数字标题无重复；
- `spec\protocol-v1.md` 不存在。

排除上层 2–10 后的范围内检查也通过：283 个链接无缺失、17 个 JSON 无错误、标题无重复。

## 四、最终意见

> **批准当前 10 份 Normative Spec。上层 2–10 不在本审批范围内。**
