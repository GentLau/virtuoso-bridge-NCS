# 当前版本验收审批意见（Spec 专项复审·再审）

> 审批日期：2026-09-11
> 复审快照：`2ae94c09112c641936e53ae9ea98832040ba4af1`
> 详细报告：`C:\Users\user\Desktop\repos_Github\virtuoso-bridge-NCS\doc\设计规格全量复审意见.md`
> Spec manifest hash：`94dd0d69c8c741101f61076245b4f40c92b0e213e1846cc8f0c0920ff325ab5e`
> 本次范围：重构后 Spec 的冲突、完备性、唯一 owner、重复机制、索引质量，以及对新 `src/` 的有限可实现性核对。
> 说明：`C:\Users\user\Desktop\repos_Github\virtuoso-bridge-NCS\spec\protocol-v1.md` 已删除且不纳入评审；`src_bak/`、旧 `test_bak/` 不作为新实现证据。

## 一、审批结论

> **Spec 冻结验收：不通过（Reject / Rework）。**

上一轮提出的主要 P0/P1 已基本完成收口：参数双层语义、one-token-one-CIW、Spectre 非阻断策略、运行期检查矩阵、错误类别、registry 生命周期、日志 metadata frame、canonical schema 方向和 manifest 均已补入。

本轮仍存在三个会影响最终实现的 P0：

1. Spectre 非阻断策略与“全 endpoint host-key mismatch 必须 ERROR”冲突；
2. canonical 注册 schema 仍有缺失字段、非法 JSON 示例和不完整 fingerprint 示例；
3. channel budget 的 per-token/per-endpoint 作用域与 SSH forwarding channel 记账不一致。

此外仍有 daemon user/run-as、split-host 文件根、endpoint key 序列化、`CommandResult.kind` 落地、timeout/retry、remove/revoke 术语及外围调用指南过期等 P1 问题。

## 二、代码与测试审批边界

本次是 Spec 再审，不替代完整代码和测试验收。有限交叉核对显示当前新 `src/` 尚未完全实现最新 Spec：

- `CommandResult` 仍未包含规范要求的 `kind`；
- role user 字段尚未进入新 registry model；
- Spectre 探测失败仍有阻断路径；
- registry 仍未体现规范所述跨进程 OS 文件锁；
- channel budget 仍是 token 级 semaphore；
- 仍保留单值 host-key fingerprint 双轨字段。

当前工作树还有未提交脚本/测试变更，因此不能把既有测试执行报告当作本次 Spec 通过证据。

## 三、重新送审条件

1. 关闭详细报告中的 P0/P1，并在同一稳定 commit 上更新 Spec manifest；
2. 修正 canonical schema、合法 JSON 示例、所有 endpoint fingerprint 和 Spectre host-key 规则；
3. 统一 channel budget、forwarding channel、`ssh.max_sessions` 和 endpoint 复用定义；
4. 将新 `src/` 的结果模型、role user、Spectre warning、OS registry lock、per-endpoint budget 与 Spec 对齐；
5. 更新 `C:\Users\user\Desktop\repos_Github\virtuoso-bridge-NCS\doc\接口调用指南.md` 及 Research 历史路径/旧日志算法副本；
6. 在最终干净 commit 上重新运行代码契约、完整测试和覆盖率，并重新审核 `doc/` 测试报告。

**当前意见：退回规格收口；不批准 Spec 冻结，不批准基于该 Spec 的最终项目验收通过。**
