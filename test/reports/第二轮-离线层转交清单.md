# 第二轮测试 · 离线层转交清单（2026-09-23）

> 触发：设计侧提交 `b36bade`（批量修复首轮转交缺陷）后，离线三层实跑
> **1336 采集 / 23 红**。逐条定性后：**23 条全部是"红灯用例写死了修复前行为"，
> 未发现新的源码回归**。本清单交给设计侧更新 TB；改完离线层回绿，我接着跑半真机层与真机层。

复跑口径（仓库根目录）：

```powershell
python -m pytest test/offline/unit test/offline/integration test/offline/scenario -q -p no:cacheprovider
```

## A. 只差摘掉 `@unittest.expectedFailure`（7 处，改完即绿）

| 文件:行 | 用例 | 缺陷 | 动作 |
|---|---|---|---|
| `test/offline/unit/test_schematic_contracts.py:364` | `test_check_and_save_must_report_check_failure` | 0584e46b | 去掉装饰器（断言已是修后契约） |
| `test/offline/unit/test_schematic_contracts.py:376` | `test_write_must_report_check_failure` | 0584e46b | 同上 |
| `test/offline/unit/test_spectre_util_contracts.py:250` | `test_noise_integral_empty_window_should_fail` | b2898828 | 去掉装饰器 |
| `test/offline/unit/test_maestro_command_exprs.py:214` | `test_set_spec_should_accept_numeric_bound` | 012d9151 | 去掉装饰器；建议改名 `test_set_spec_accepts_numeric_bound` |
| `test/offline/unit/test_verilog_contracts.py:71` | `test_imported_cells_should_dedupe_cleaned_names` | 70645f43 | 去掉装饰器，并删掉 `:65` 那条写死旧行为的重复用例（见 B） |
| `test/offline/unit/test_veriloga_contracts.py:116` | `test_neither_target_should_raise` | e913012c | 去掉装饰器 |
| `test/offline/unit/test_veriloga_contracts.py:124` | `test_both_targets_should_raise` | e913012c | 去掉装饰器 |

## B. 断言写死了修复前行为（改断言）

| 文件:行 | 用例/位置 | 现在断言 | 应为 |
|---|---|---|---|
| `test/offline/unit/test_maestro_command_exprs.py:224` | `test_set_spec_numeric_bound_currently_raises_attribute_error` | 期望 `AttributeError: 'int' ... 'replace'` | **删除**该用例（修后不再抛，且它描述的是缺陷本身） |
| `test/offline/unit/test_spectre_util_contracts.py:237` | `test_noise_integral` 的空窗口段（245–248） | `ok=True`、`value=0.0` | `ok=False`、`detail` 含 `window is empty` |
| `test/offline/unit/test_verilog_contracts.py:65` | `test_imported_cells_dedupes_and_strips_punctuation` | `["counter8","counter8","counter8"]` | `["counter8"]`（清洗后再去重） |
| `test/offline/unit/test_verilog_contracts.py:440` | `test_short_view_entry_is_rejected_as_index_error` | `ok=False` 且错误含 `IndexError` | `ok=True`、`views == []`（3 元素畸形条目按跳过处理） |
| `test/offline/unit/test_calibre_argv_contracts.py:88` | `test_pex_adds_a_format_stage_only_for_spice_like_formats` | `["calibre","-xrc","-fmt","-spice",…]` | 取值去掉前置短横线：`-fmt spice` / `-fmt simple`（calibre 只认裸格式名） |
| `test/offline/unit/test_calibre_package.py:121` | `FakeMiddle.download_file` | 只对 `pex.stage2.log` 喂 `PEX_LOG` | 也要对 **`pex.stage3.log`** 喂（`read_results` 现在优先 stage3）；注意修后 `job.json.fmt∈{spice,simple}` 还要求日志里有 netlist 文件 |

## C. 假夹具要跟上新代码（layout）

| 文件:行 | 现象 | 应改 |
|---|---|---|
| `test/offline/unit/test_layout_contracts.py:395` `OrchestrationMiddle` | 修后 `_is_locked()` 会调 `middle.run_command` 探测 `*.cdslck`；假夹具没有该方法 → `AttributeError` 被包成失败（3 条 write 用例全红） | 给假夹具加 `run_command`：无锁回 `rc=0`+空 stdout，有锁回一行 `*.cdslck`；顺带补一条"持锁必须先报锁、不落任何写操作"的用例覆盖 83ee8603 |
| `test/offline/unit/test_layout_contracts.py:509` `test_command_failure_mentions_non_transactional` | `command_ok=False` 现在会先把锁探测打挂（探测走同一兜底分支），错误变成 `RuntimeError: command boom` 而缺 "not transactional" | 把失败限定在"应用命令"那一步（只在 SKILL 含 `vbLayoutCv` 时返回 FAILURE） |

## D. `netlist.import`：5 条用例基于已删除的"假成功"路径

`src/pyapi/packages/netlist_import.py` 现为**参考桩**（修复 202c4e37）：不调中层、不落任何 cell/view/symbol，
直接 `ok=False` + `"reference stub"`。`test/offline/unit/test_netlist_import_package.py:55-94` 的 5 条仍在断言
upload→import→symbol 的假成功链路，需重写为：

- `ok is False`、`steps == []`、`error` 含 `reference stub` 与 `no cell/view/symbol`；
- **零副作用**：`middle.calls == []`（这是该修复的核心保证，必须钉住）；
- 保留 `test_validation_failures` / `test_self_description_exports`。

## 备注（不是缺陷）

- 本轮我清掉了 `test/**/__pycache__`（10 个目录），此前 traceback 里出现过搬迁前的旧路径，属陈旧字节码，与代码无关。
- 离线层通过后，我按 `test/docs/推荐测试环境.md` 起半真机/真机环境继续。

## E. 另有一条 P1 **源码缺陷**（不是 TB 问题，但请一起修）

`src/bridge/resources/ramic_bridge_daemon_27.py` 缺 `# -*- coding: utf-8 -*-`：
文件里有 12 行中文注释，py3 默认 UTF-8 所以一直没暴露，**真 Python 2.7 下直接 SyntaxError、daemon 起不来**
（复现：`python2.7 ramic_bridge_daemon_27.py 127.0.0.1 <port> <token> <tmp>` → `SyntaxError: Non-ASCII character`）。
远程副本只加这一行后，真 py2.7 探针 **5/5 PASS**（`test/semi/probes/py27_daemon_probe.py`，
证据 `test/artifacts/evidence/py27-daemon-probe.json`）。修好后通知我在 wsl-gent 上复跑落绿证据。
