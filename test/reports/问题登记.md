# 问题登记（测试架构级）

> **2026-09-23 结构变更**：`test/` 已按三级物理重组（`test/offline`、`test/semi`、`test/live` + 共享的
> `support/`、`runners/`、`standards/`、`artifacts/`、`legacy/`）。本台账中 **2026-09-23 之前**的记录保留
> 当时的路径字样（含 `test/tb/*`）——它们描述的是当时的事实，不做改写；新条目一律用新路径。

> 维护者：测试工程师 ｜ 版本：v1.0 ｜ 2026-09-22
> 规则：登记**测试架构/覆盖度/证据链/文件纪律**层面问题，以及本轮测试发现的**产品缺陷**（含 bug API 编号）；
> 发现即记录，先不修；每条必须有可复核证据；修复后状态改"已闭环"并补证据。

## 1. 待处理

| ID | 类别 | 问题 | 证据 | 影响 | 状态 |
|---|---|---|---|---|---|
| P-001 | 覆盖度报告 | `doc/测试覆盖报告.md` 与当前 `src/` 布局脱节：表中仍写 `transport/register/flow.py`、`server/registration_server.py`，而注册已独立为 `src/register/`；收录数写 456，实测三层 765（unit 720 / integration 29 / scenario 16）+ 真机 10（`test/live/e2e`，2026-09-22 `--collect-only`） | `rg -n "transport/register\|server/registration_server" doc/测试覆盖报告.md`；`pytest --collect-only` | 送审材料与代码对不上，最易被驳回 | 待修（需指定负责人） |
| P-002 | 文档漂移 | `AGENTS.md` 13 处引用 `skills/`（目录已并入 `skills_bak/`） | `Test-Path skills` → False | — | **已关闭**：用户确认 `AGENTS.md` 属重构前残留，不在测试范围 |
| P-003 | 环境占用 | 固定端口 `8124`（`register.server`）/`8127`（`api_server`）长期被残留进程监听；跑测前不查会失败或假绿 | 2026-09-22 17:29 起监听，17:52 复测仍在：8124=pid19468、8127=pid31740，且 `.venv` 与系统 Python 各有一套 supervisor/api_server/register.server（共 6 个常驻进程） | 跑测结果不可信 | 待处理（已写入跑测前检查清单） |
| P-004 | 证据噪声 | `.pytest_cache/v/cache/lastfailed` 66 条记录里 14 条指向已不存在的路径（`tests/`、`test_bak/` 等旧布局） | 解析脚本输出 | 误导排查；缓存不入库故影响有限 | 已记录（建议定期清缓存） |
| P-005 | 瞬态红灯 | `test/offline/unit/test_calibre_package.py` 4 个用例曾报 `RuntimeError: work dir not initialized`（`pyapi/packages/calibre.py::_staging_dir`），同命令复跑 151/151 全绿 | 两次运行输出对比 | 疑似设计工程师编辑期的瞬态；需在稳定后复验一次 | 观察中（复验后闭环或升级为缺陷） |
| P-006 | 文件纪律 | 路径使用专项审计：四条问题（见 §1.1）尚无结论；"是否有文件泄露"直接影响文件纪律与资源回收两栏的验收结论 | 审计报告 [audits/路径专项-2026-09-22.md](路径专项-2026-09-22.md) | 决定"文件泄露"结论 | **首轮完成（部分结论）**：A/B/C/D 已逐条给出结论表；Q1 全链路、Q2 逐 role、Q3 逐 TB 三项标注未覆盖 |
| P-007 | 范围口径 | 口径冲突：`test/plans/总览与执行约定.md` §1 写"上层业务内部实现不在范围内"，而 [测试架构.md](../docs/测试架构.md) §1/§3 要求上层包的纯逻辑工具必须有离线契约 | 两处原文 | 直接影响"缺哪些测试"的判定与审核结论 | **已定稿**：以 [测试架构.md](../docs/测试架构.md) §1 为准（内部实现不测；纯逻辑工具与五接口消费路径要测）；余项：`test/plans/总览与执行约定.md` §1 措辞待同步 |
| P-008 | 仓库卫生 | `test_bak/`、`src_bak/`、`skills_bak/`、`build/` 等重构前目录 | `git ls-files test_bak src_bak` | — | **已关闭**：用户确认带 `bak` 的目录属重构前代码，不关注 |
| P-009 | 目录可读性 | `test/` 下 `artifacts/`、`frontend/`、`standards/` 三个子目录就地没有 README（职责已在 `tb/README.md` §1 表格覆盖） | `Get-ChildItem test/tb -Directory` 逐一检查 `README.md` | 审核者翻目录时缺少就地说明（低） | **已闭环（2026-09-23）**：`test/artifacts/README.md`、`test/offline/frontend/README.md` 已补（`test/shared/standards/` 只有一份自述指针文件，不另加） |
| P-010 | 文档一致性 | [文件使用规范.md](../docs/文件使用规范.md) §7 把"字面量 `~` 目录""路径拍平目录"写成**已清理**，现场仍在盘上；**现场观察，非判定证据**（尚未定位到产生它们的代码） | 2026-09-22 `ssh wsl-gent`：`/home/Gent/~`（09-14）、6 个 `homeGent.virtuoso-bridgee2e-*`（09-09） | 规范与现场不一致；若能定位到仍会复现的代码，才升级为真违规 | 待处理（先把 §7 改为"已确认发生、待清理"；能否复现交代码审计定性） |
| P-011 | 口径冲突 | `/tmp` 部署形态存在**规范 vs 实现**冲突：`AGENTS.md`/实现把 bridge 部署在 `/tmp/virtuoso_bridge_<remote_user>/<client_id>/`，而 [文件使用规范.md](../docs/文件使用规范.md) §1/§4.8 写明"禁止共享 `/tmp`、远端只写 role root" | 2026-09-22 17:50：`ls -1d /tmp/virtuoso_bridge_*` → `/tmp/virtuoso_bridge_Gent`；同次盘点 `/tmp` 下 `vb_*` 条目 **492** 个（含 `/tmp/vb_docq_index.json` 2.1 MB、`/tmp/vb_pipeline_out.txt`），最晚 **2026-09-22 17:09** 仍在增长 | 同一条 `/tmp` 在两份文档下结论相反，"是否泄露"无法判定 | 待定稿（先由路径专项审计给事实，再统一口径：设计内的 `/tmp` 记为"已知例外"，其余判泄露） |
| P-012 | 跨层落盘 | L1 用例把临时目录建进 TB 证据树且不回收 | `test/offline/integration/test_supervisor_process.py:47-50`、`test_business_child_supervision.py:26-29` | — | **已闭环（18:45）**：改系统 temp + `vb-` 前缀；18:36–18:39 全量跑测未再在证据树新增残留 |
| P-013 | 文件泄露（客户端·有代码锚点） | 客户端未完全遵守注入的 work-dir：①`middle` 层在**未注入 `err_dir`** 时回退系统 temp；②隧道 stderr 日志在**成功路径**不回收 | 代码（快照 2026-09-22 18:27）：`src/transport/middle.py:204`（`else` 分支 `mkdtemp(prefix="vb_local_err_")`；`:202` 支持 `dir=err_dir`，生产调用点 `:685` 传 `err_dir=temp_dir()`）；`src/common/ssh.py:586-591`（`delete=False` 且无 `dir=`），失败路径有 `os.unlink`（`:644`），**成功路径 `return`（`:620-621`/`:625-626`）不删且路径未登记**，`stop_port_forward:721` 无从清理；`ssh.py:237` 属设计内例外。详见 [audits/路径专项-代码合规-2026-09-22.md](路径专项-代码合规-2026-09-22.md) A-1/A-2 | A-1 是潜在分支（当前生产路径不触发）；A-2 每成功建一次隧道留一个 `.log` | 待处理（A-1：回退 work root 或改必填；A-2：落 work root，或把路径记进 runner 状态、在 teardown 删除） |
| P-018 | 文件泄露（测试侧·有代码锚点） | 测试的临时目录清理机制覆盖不到无前缀调用：conftest 只匹配 `vb-*`，而 `mkdtemp()` 无前缀时生成 `tmpXXXX` | [路径专项-代码合规-2026-09-22.md](路径专项-代码合规-2026-09-22.md) A-3（真漏点 **23 文件 / 76 处**；A-5 已撤回，`tb/core` 三脚本本就自清理） | — | **已闭环（18:45）**：121 处加 `prefix="vb-"`，conftest 改 session 级回收；隔离 TEMP 复跑残留 0。注：两条走 bug API 的测试侧报告（`bug-…f788c4e9`、`bug-…d57619ff`）已由本次直接修复取代，其中 A-5 判断有误已在审计报告更正 |
| P-019 | 健壮性缺陷（源码·已复现） | **孤立代理项**（`"\ud800"`）请求字段 → HTTP 面**断连**（无状态码、无 body），而非 4xx | 复现件 `test/semi/probes/lone_surrogate_probe.py`（FAIL 3/3）；traceback：`src/server/api_server.py:75` / `src/register/server.py:77` 的 `json.dumps(..., ensure_ascii=False).encode("utf-8")` → `UnicodeEncodeError: surrogates not allowed`；对比：深嵌套 20000 层已正确返回 400 | 违反 `src/common/jsonutil.py` 文档承诺；任意客户端可触发无响应断连，不可归因 | **已报 bug-20260922T104416Z-vblog-4ed06a1e**；待设计工程师修复（建议解析侧拒绝 + 序列化侧兜底，修复后把 probe 提升为 `test/offline/integration` 门禁用例） |
| P-020 | 契约不一致（源码·已复现） | `spectre.measure` 在零幅度 AC 点输出 `-Infinity`：响应体不是合法 JSON，桥自带的 `loads_strict` 会拒收 | `src/pyapi/packages/_spectre_util.py:679`（`value = 20*log10(mag) if mag>0 else -math.inf`）；`json.dumps` 写成字面量 `-Infinity`；`common.jsonutil.loads_strict('{"v": -Infinity}')` → `ValueError` | 入参侧严格、出参侧不严格；严格客户端解析失败 | **已报 bug-20260922T105143Z-vblog-f085537a**；待设计工程师修复 |
| P-021 | 环境/目录纪律（真机·已取证） | **Virtuoso 启动位置不规范**：历史实例用 `~/project/<user>/` 甚至 `$HOME` 作 cwd，运行态文件与陈旧锁散落 | 实测（wsl-gent 2026-09-22）：`pid=369800 cwd=/home/Gent/project/vblog`；73 个工程目录中 68 个含 `CDS.log`、69 个含 `start*.log`；`$HOME` 根有 `CDS.log`、`.artist_states/`、14 个 `*.ahdlSimDB/`；陈旧锁 `~/CDS.log.cdslck`(09-21)、`project/vb01/CDS.log.cdslck`(09-15)、`project/vblog/CDS.log.cdslck`(09-18)；字面量 `~` 目录由 `test/shared/archive/composite_stress.py:91` 产生 | 家目录/工程目录被运行态污染；陈旧锁会让后续启动失败（历史反复重启的根因之一） | 规范已落到 [环境建设说明.md](环境建设说明.md) §1（启动 cwd=专用 run 目录、锁检查、日志归位）；现场清理与 legacy 重写列为待办 |
| P-022 | 环境事实（lab） | **lab WSL 空闲 1–2 分钟自动停机**：身份 IP 与 ssh 一起消失；fake 实例由 root（sudo）持有，停止必须 kill daemon 子进程 | 实测：18:47 启动 w1 → 18:56 已 `Stopped`；`pkill` 模式写 `127.0.0.1` 匹配不到（真实 argv 是 `ramic_bridge_daemon_3.py 0.0.0.0 <port> …`） | 不保活的环境会在跑测中途失联；停不干净的实例会占端口 | **已闭环（工具侧）**：`test/shared/runners/env_up.py` 内置保活 + 子进程精确回收，实测 up 2/2 → status UP → down 2/2 → status DOWN；细节见 [环境建设说明.md](环境建设说明.md) §4 |
| P-023 | 资源上限（已核算） | **"wsl-gent 起 20 个真 Virtuoso" 不可行**：单实例 RSS 0.9–1.0 GB × 20 ≈ 18–20 GB > 物理 15.9 GB（当前可用 13.3 GB） | `ps -o rss` 实测 971 MB / 820 MB；`free -m` 15911/13260 | 需求若写死"20 真机"则无法满足，须降规模或加机器 | 内存账见 [环境建设说明.md](环境建设说明.md) §3；对使用者的口径写在 [docs/推荐测试环境.md](../docs/推荐测试环境.md) §2 备注（真机上限 10–12，S4 用"真 4 + fake 16"）；**待用户确认是否接受替代** |
| P-024 | 能力（已实测） | 场景 S1（Calibre/带工艺库真机）的起法此前未验证 | 2026-09-22 实测 `envcheck`：`virtuoso -cdslib <project>/cds.lib` 在 `~/.virtuoso-bridge/<u>/run/` 作 cwd 启动；运行态文件全落 run 目录、设计库命中 5 处、`$HOME/CDS.log` 未被改写；实验目录已清理 | — | **已闭环**：验证记录见 [环境建设说明.md](环境建设说明.md) §7 |
| P-025 | 缺陷（源码·已复现，本轮 B3 发现） | **Windows 上多进程共享 work-dir 时 `log/commands.log` 轮转失败**：`RotatingFileHandler` 用 `os.replace` 重命名，另一进程持有句柄即 `WinError 32`；轮转中止、文件无限增长，异常只在 logging 线程打 traceback | 最小复现 `test/semi/probes/log_rotation_lock_probe.py` → FAIL（目录里只有 `commands.log`、无 `.1`，停在 5,100,155 字节）；真机 traceback 见 `log_matrix_real_tb.py` 运行输出（paramiko 日志线程 `PermissionError: [WinError 32] ... commands.log -> commands.log.1`） | 多进程共用 work-dir 是桥自身用法（控制面/业务面/本地客户端），会静默丢日志/撑爆磁盘 | **已报 bug-20260922T120212Z-vblog-f8b17889**；待设计工程师修复（跨进程安全轮转或降级策略） |
| P-014 | 现场观察（**已降级**） | Windows 家目录根散落 11 个 vb 目录（`vbtest`、`vb-multi-*` 等）——**无代码锚点**，未定位到产生它们的代码 | 2026-09-22 `Get-ChildItem $env:USERPROFILE` | 不能据此判定当前代码违规（可能是旧版本/一次性脚本留下） | 待代码定位（找到创建者再立违规条目；定位不到就只做清理） |
| P-015 | 覆盖度/架构风险 | 同名 SKILL 文本工具**双实现**（`basic.py` vs `_maestro_util.py`），单测只覆盖 basic 那份 | 2026-09-22 复核：`_maestro_util.py:19` 已改为 `from ...basic import parse_sexpr`，`maestro.py:38` 直接用 basic 的 `parse_sexpr/q`，重复实现已收敛 | 单测绿灯曾对 maestro 路径是假信心 | **已闭环**（设计工程师提交 24333c0 收敛；本轮复核见行号） |
| P-026 | 缺陷（源码·已复现，本轮 B3 发现） | **maestro 会话匹配用子串**：`view="maestro"` 命中库名 `maestro_tb` 的 Reading 窗口 → 走 reading 分支调 `maeMakeEditable` 失败 → `maestro.run` 整体报错 | `src/pyapi/packages/maestro.py:472-486`（`library/cell/view in title` 子串判断）、抛错点 `:572-579`；窗口快照：`("fnxSession160" 23 "…Reading: maestro_tb opamp_probe schematic…")` 与 `("fnxSession160" 12 "…Editing: maestro_tb opamp_probe maestro")` 并存 | 同名/子串命名（库 maestro_tb、view maestro）在真机很常见，一旦并存就必然失败 | **已报 bug-20260922T121908Z-vblog-b06063ab**；修复建议：解析标题结构（Editing/Reading 后三个 token 逐字段相等），不要 `in` 子串匹配 |
| P-027 | 缺陷（源码·已复现，本轮 B4 发现） | **`virtuoso.netlist.import` 假成功**：对**不存在的目标库**仍返回 `ok=true`；三步（upload/import/symbol）全绿，实际 SKILL 只是 `printf("import lib=…")` / `printf("symbol lib=…")`，不建 view、不建 symbol | `src/pyapi/packages/netlist_import.py:70-90`；证据 `test/artifacts/evidence/s11-probe/netlist-import-false-success.json`（import → ok=true；同库 `view.list`/`cell.list` → `libraryNotFound`）；运营面 `/help` 显示它与其余 81 个生产操作并列 | 静默假成功是最危险的失败模式：调用方以为网表已导入，后续 symbol/LVS/仿真全部建立在错误前提上；现网 TB `infra_e2e_tests.py:157-161` 只断言 `ok`，形成假信心 | **已报 bug-20260922T123859Z-vblog-202c4e37**；修复建议：摘出运营面，或真正导入并用 `view.list` 校验后再返回成功；TB 断言改为「view 真实存在且内容非空」 |
| P-028 | 环境/能力缺口（本轮 B4 实测） | **运行中的 vblog CIW 没有 PDK**：其工程 `cds.lib` 只有 SOFTINCLUDE，CIW 启动后新增的 `DEFINE tsmcN65` 不会被热加载（`ddGetObj("tsmcN65")~>name` → `nil`）→ 新建库拿不到 PDK tech → 布局层 `M1` 不存在（`dbCreateRect: Invalid layer/purpose`）→ xstream 用 PDK layermap 报 `XSTRM-32 unknown layer 'SRM'` → DRC/LVS 无法进行 | `ssh wsl-gent grep tsmcN65 ~/project/vblog/cds.lib`（补 DEFINE 前为空）；`test/shared/runners/skill_eval.py --token vb-vblog 'ddGetObj("tsmcN65")~>name'` → `nil`；S11 阶段证据 `test/artifacts/env/scenario-s11/s11-{schematic,layout,gds}.json` | 任何"带工艺库的端到端流程"都必须跑在**启动时即加载 PDK 的会话**里；S11 因此在本轮未能走通 LVS/仿真段 | 处置：按 [推荐测试环境.md](../docs/推荐测试环境.md) S1 起**独立 PDK 用户实例**（cwd=`~/.virtuoso-bridge/<u>/run/`、`-cdslib` 指向含 PDK 的工程），再跑 S11；已备份现场 `~/project/vblog/cds.lib.bak-s11` |
| P-029 | 缺陷（源码·已复现，本轮 S11 发现） | **`virtuoso.layout.gds` 导出忽略 `file_is_local=False`**：靶机产物被"下载"到客户端的假路径树（`C:\home\Gent\...`），靶机上不留文件，返回值仍报 `reason=completed` | `src/pyapi/packages/layout.py:1061`（export 分支调 `_publish_remote(remote_gds, output_path, request)`）、`:1219-1238`（`_publish_remote` 无条件在客户端建目录 + 下载，不看 `file_is_local`；该字段定义在 `:84`） | 调用方"文件留在靶机"的意图被静默忽略 → 客户端文件系统被写入陌生绝对路径树（`C:\home\Gent\.virtuoso-bridge\vbs11\tmp\cmp_top.gds` 2048B + 205KB 日志，**现场保留作为证据**）；后续依赖靶机产物的步骤（LVS 取 GDS）必然失败 | **已报 bug-20260922T123712Z-vblog-afac3523**；修复建议：按 `file_is_local` 分流，或明确报"该参数在导出方向不支持"+ 客户端路径合法性校验（编号修正：本条原误用 P-027，与 netlist.import 冲突） |
| P-030 | 缺陷（源码·已复现，本轮 B2 发现） | **`schematic.set_term_nets` 默认 `stub_length=0.5` 对 65nm PDK 过大**：`schCreateWire(entry=route)` 连不上，端子被路由到 `net1/net2` 等错误网络；接口仍 `ok=true`、无 warning | `src/pyapi/packages/schematic.py:469`（默认值）、`:476-483`（stub wire + label）；真机对照见 `test/artifacts/env/scenario-project65/evidence-schematic*.json`（stub 0.5 vs 0.05 的 term→net 映射不同） | 生成的原理图连接关系错误且静默通过，会把错误带到 CDL/LVS/仿真 | **已报 bug-20260922T122850Z-vblog-b7ed6176**；建议默认值改为与 PDK 无关的安全策略（由调用方显式给，或按 tech 的 minWidth 计算） |
| P-031 | 缺陷（源码·已复现，本轮 B2 发现） | **schematic `check_and_save` / `write` 忽略 `schCheck` 失败**：SKILL 返回 `"check-failed"` 时仍 `ok=true`（layout 同路径有 `"saved" in output` 校验，属实现不一致） | `src/pyapi/packages/schematic.py:410`、`:690-693`、`:756-759`；L0 复现 `test/offline/unit/test_schematic_contracts.py`（`unittest.expectedFailure`×2） | 原图电气检查失败（悬空/短路/未连线）被静默当作成功保存 | **已报 bug-20260922T124018Z-vblog-0584e46b**；建议比对 `output` 是否含 `"saved"`，与 `layout.py:490` 对齐 |
| P-032 | 缺陷（源码·已复现，本轮 B2 发现） | **`verilog._imported_cells` 去重顺序错误**：用未清洗 token（`counter8,`）与已清洗列表（`counter8`）比较 → 同一 cell 多视图导入时重复计入 | `src/pyapi/packages/verilog.py:637-643`；L0 复现 `test/offline/unit/test_verilog_contracts.py`（`expectedFailure`） | 接口返回的 `imported_cells` 元数据重复；调用方统计/后续处理受污染（当前无真实 VERILOGIN 日志样本，可达性待现场确认） | **已报 bug-20260922T124456Z-vblog-70645f43**；建议先清洗再比较 |
| P-033 | 仓库卫生 / 凭据（测试侧·待处理） | **"artifacts 已 gitignore"与事实不符**：`.gitignore:27` 忽略 `test/artifacts/`，但其中 **231 个文件已被跟踪**（忽略规则不追溯已跟踪文件），且多为每次跑测都会改写的产物——包含 `log-vblog/registry.json`（含 calprobe daemon token 明文）、`lay_e2e.gds`（二进制）、`http-stress-sat/local-root/file/*.bin` 等 | `git ls-files test/artifacts \| wc -l` = 231；`git status` 中大量 ` M test/artifacts/...`；`log-vblog/registry.json:288` | ① 每次跑测污染 diff，审核无法区分"本轮证据"与"历史产物"；② daemon token 随产物入库（与 C-013 同类但面向 daemon 而非 admin）；③ 二进制 GDS/bin 入库使仓库膨胀 | 待处理：按 `docs/证据与报告.md` 的 `git add -f` 规则**白名单保留**（建议只留各场景的 `evidence*.json` + `results.json`），其余 `git rm --cached`；是否执行需用户确认（涉及版本库变更） |
| P-034 | 缺陷（源码·已复现，本轮 B4/S11 发现） | **`calibre.pex` 第三阶段参数错误且失败被静默**：`_argv_for` 生成 `calibre -xrc -fmt -spice run_pex.cal`（正确形态是 `-fmt spice`，格式作为 `-fmt` 取值）→ 工具打印 usage 退出、`pex.log=stage3_failed`；而 `read_results` 的 pex 报告候选只有 `pex.stage2.log`/`pex.stage1.log`（不含 stage3），`status` 也不读 `pex.log` 的 stage 标记 → `read_results` 返回 `ok=true, errors=0, netlist_files=[]` | `src/pyapi/packages/calibre.py:680`（stage3 argv）、`:437-438`（报告候选）、`_calibre_util.py:209-216`（netlist 只从所读日志正则）；现场 `s11-postsim`：`launch.sh:10` 实际命令、`pex.log=stage3_failed`、`pex.stage3.log` 从 108 行起为 Usage；`read_results(kind=pex)` → `netlist_files=[]` | 请求 `fmt=spice` 的后仿网表时得到"成功但无网表"：PEX 唯一产出可仿真网表的阶段永久不可用，后仿（含寄生）被静默阻断 | **已报 bug-20260922T130754Z-vblog-836e3215**；建议 ① argv 改 `-fmt <fmt>`；② 候选加入 `pex.stage3.log` 并把 `*_failed` 标记纳入失败判定；③ fmt 请求了 spice/simple 而 `netlist_files` 为空时必须报失败 |
| P-035 | 测试数据（S11·已闭环） | S11 的 `cmp_top`/`inv2` CDL 使用 `l=180n`，低于 PDK 所有模型 bin 的 Lmin（`nch_25` 280n、`nch_18` 260n）→ Spectre 直接报 CMI-2440 无法选模型 | 真机 `spectre.out`：`ERROR (CMI-2440) ... w=2e-06, l=1.8e-07 ... Lmin=2.6e-07/2.8e-07` | 若演示设计不改尺寸，任何"带 PDK 的仿真"都要额外处理 | 已闭环（测试侧）：前后仿 netlist 统一加 `simulatorOptions options soft_bin=allmodels`（工具自身建议），pre/post 同口径可比；建议后续把演示单元尺寸改到合法区间 |
| P-036 | 环境/工具（测试侧·**2026-09-23 复测后重判**） | **lab fake 实例与 bridge 隧道不兼容**（原判）：`env_up.py` 在 lab WSL 用 `sudo ip netns exec labns` 起 fake（fleet 记录 `ip=172.20.170.21`），而 bridge 隧道只转发 `127.0.0.1:<port>`，客户端连上后协议无应答 | 复测（2026-09-23 01:5x，同一 S3 注册表、仓库内 TB `test/live/stress/multi_user_routing_tb.py`）：w1:65201 / w2:65301 两个 lab fake **经隧道可达**——5/5 用户串行 `RBDToken` 自证正确、并发 10/10 ok、0 failed / 0 misrouted（`test/artifacts/env/scenario-multi-user-s3/evidence-rerun-20260923.json`） | 原"不可用"结论对**当前部署不成立**；早期 `Empty response`/超时更可能由 fake-CIW 成帧缺陷（09-22 已修，见报告 §4.2）导致，netns 归因被推翻 | 待办降级：不再阻塞任何场景；仅需复核 `env_up.py:125` 的 netns 启动路径在新环境下是否仍会产生不可达实例（当前实测可达，未复现原症状） |
| P-037 | 缺陷（源码·已复现，本轮 S2 发现） | **paramiko 后端把 `ssh -G` 的 `true/false` 当非法值**：`~/.ssh/config` 里 `StrictHostKeyChecking yes` 的主机一律连不上，且报错误导为"要求预录主机密钥"（密钥其实已在 known_hosts） | `src/common/paramiko_backend.py:738-747`（`lookup.get("stricthostkeychecking")` 直接比较 `not in ("yes","ask")`）；实测 `ssh -G w1-gent` → `stricthostkeychecking true`；S2 跨主机 command role → `kind=transport rc=255`，stderr `... StrictHostKeyChecking=true is not supported for host 'w1-gent'`（`test/artifacts/env/scenario-role-split/{five-interface.json,run3.log}`） | 任何按安全合规把该值设为 `yes` 的用户都无法使用 paramiko 后端；归因误导 | **已报 bug-20260922T133620Z-vblog-43d5b77d**；建议归一化 `true→yes`/`false→no`、只拒绝 `no/off/accept-new`，并在报错里回显原始值。环境绕行：改 `ask`（密钥已预录）后 **S2 五接口全过** |
| P-038 | 缺陷（源码·已复现，本轮 S6 发现） | **声明支持 Python 3.9，但客户端在 3.9 上无法导入**：pydantic 求值 PEP 604 注解（`X | None`）直接抛 `TypeError`；`pyproject` 写 `requires-python>=3.9`、CI 矩阵含 3.9，而 dev extra 未含 `eval_type_backport` | `src/common/registry.py:155`（`mode: Literal["local","remote"] | None = None`，顶部 `from __future__ import annotations`）；全仓 48 文件 785 处 PEP 604 注解；w1-gent（Python 3.9.25）跑 S4 同一 TB → `TypeError: Unable to evaluate type annotation "Literal['local','remote'] | None" ... install the eval_type_backport package` | 任何 3.9 客户端（CI 3.9 作业、lab w1）不可用；跨客户端一致性在 3.9 上不成立 | **已报 bug-20260922T141119Z-vblog-5e939e33**；建议二选一：`requires-python>=3.10` 并删 CI 3.9，或加 `eval_type_backport; python_version<"3.10"` 并让 3.9 作业真跑绿。S6 用 backport 绕行后 **Windows/Linux 双端各 1600 调用 0 失败** |
| P-016 | 完成度评估 | **离线层零引用**的模块 5 个：`common.jsonutil`、`pyapi.packages._spectre_util`、`_skillref_docs`、`_symbol_generate`、`_maestro_util`；另 `layout/schematic/maestro/verilog/veriloga/symbol/cellview` 仅真机 TB、无离线契约。其中 `loads_strict` 是顶层 HTTP 入参解析器（`register/server.py:109`、`server/api_server.py:100`）却零测试 | [覆盖度缺口.md](覆盖度缺口.md) §2；`rg -c -e "\bjsonutil\b" test/offline/unit test/offline/integration test/offline/scenario`（无输出） | 审核问"哪些没测"时无据可依；且这批纯文本/结构处理**不需要真机**即可覆盖，属性价比最高的补测区 | 待处理（测试工程师，按 §2 建议层归口，补测计划另立） |
| P-017 | 归属未定 | 远端 `/tmp` 残留中归属不明的部分：`vb_*.sh` 20 个（09-21）、`vb_pipeline_{in,out}.txt`、`vb_test_mech_dir`、`vb_bg*`、`vb_docq_index.json`（2.1 MB） | [audits/路径专项-2026-09-22.md](路径专项-2026-09-22.md) D4 | 不能确认是 TB 还是调试脚本产物，影响"谁负责清理" | 待确认（先定创建者，再清理或登记为已知例外） |
### P-006 专项：路径与文件泄露审计（四条问题）

> 口径见 [文件使用规范.md](../docs/文件使用规范.md) §1.1：路径不是沙箱，只登记"有推荐路径可用、
> 未被显式要求、却自行另开一路"造成的混乱。**先只查不修，结论逐条回填本表。**

| 编号 | 层级 | 待答问题 | 关注点 | 状态 |
|---|---|---|---|---|
| P-006-A | 客户端 | 是否真正使用了启动时传入的 work-dir？ | 参数是否被忽略/被 cwd 覆盖；本机侧落盘位置；有无写到仓库根、盘符根、`%TEMP%` | **部分使用**：temp 类写盘未走 work-dir（结论 A1/A2/A4） |
| P-006-B | 远端 | 每个 role 是否只使用该 role 提供的 root？ | role root 与 `~/.virtuoso-bridge` 默认根的混用；跨 role 串写；他人用户目录 | **部分**：部署/注册合规（B1）；`/tmp` 部署形态口径待定（B2）、家目录残留待清（B3/B4） |
| P-006-C | TB | TB 是否遵守"客户端 work-dir / 远端 role root"？ | TB 自造路径（`/root/vbtest`、`~/.virtuoso-bridge-stress`、`~/Desktop` 等）；证据既不在 root 也不在个人目录 | **否**：裸 `mkdtemp` 落系统 temp（C1）；L1 落进证据树（C2） |
| P-006-D | 全层 | `/tmp` 残留专项 | 代码/文档/脚本对 `/tmp` 的引用与真实落盘；区分"短命通信文件（合规）"与"本该进 root 却图省事" | **有残留**：本机 3423 条 / 远端 238 条；推荐目录为空（D1–D3）；20 个 `.sh` 归属待定（D4） |

产出要求：`test/docs/audits/路径专项-2026-09-22.md`，结论表须有
`编号｜层级｜现象｜证据(文件:行)｜应写路径｜实际路径｜是否泄露｜修复建议`。

## 2. 已闭环

| ID | 问题 | 处置 | 证据 |
|---|---|---|---|
| C-001 | 重整后路径漂移：`README.md`、`test/plans/注册页与HTTP层.md` 仍指 `test/frontend_tb/`；`doc/report/环境支持.md`、`tb/fakevirt/README.md` 仍指 `test/fakevirt/` | 全部改为 `test/offline/frontend/`、`test/semi/fakevirt/` | `rg` 全仓扫描无残留（仅历史快照 `doc/report/_explore/` 保留原样） |
| C-002 | TB 脚本 docstring 仍写重整前的 `test/*.py` 路径 | 已随重整同步 | `rg "test/(semantics_tb\|cov_remote_real\|…)\." test` 无命中 |
| C-003 | `tb/fakevirt/README.md` 快速开始使用共享 `/tmp`，违反写入规范 | 改为 `~/.virtuoso-bridge/<user>/tmp` | 该文档 §2/§4 现有命令 |
| C-004 | 规范文档散落（TB 专项 + 环境 + 计划各处） | 收敛为 `test/docs/` 单一入口，旧 TB 规范降级为指针 | [README.md](README.md) |
| C-005 | `test/docs/README.md` 索引指向不存在的 `测试架构.md`、`问题登记.md`（链接 404） | 补齐两份文档；专项审计报告收敛到 `audits/`，`standards/` 只留指针 | `test/docs/` 目录清单；[audits/README.md](README.md) |
| C-006 | `test/` 与 `test/` 根目录可能残留散落脚本 | 重整后自检：`test/` 根仅 `conftest.py`+`README.md`，`test/` 根仅 `README.md` | 2026-09-22 17:55 `Get-ChildItem -File` |
| C-007 | 规范文档相对链接可能失效 | 10 份文档 41 条相对链接自检：仅 1 条指向**生成中**的 `audits/路径专项-2026-09-22.md`，其余全部有效 | 2026-09-22 17:55 链接检查输出 |
| C-008 | 分层口径与现实不符：9 个 `test/offline/unit` 文件使用真实 loopback 临时端口/本机子进程，而规范原写"L0 无真实 socket/子进程" | 澄清为 L0 例外条款（密闭本地资源允许；固定端口只可作数据，禁止 bind；新用例绑 `127.0.0.1:0`），[用例分层与归口.md](../docs/用例分层与归口.md) 升 v1.1 | `rg "\.bind\(|Popen\(" test/offline/unit`：全部为 `:0` 临时端口；`8127/65123` 仅出现在 payload/mock 数据中 |
| C-009 | 结构变更：`test/e2e` 与 `test/tb` 语义重叠（同为"真机 pytest"，且不在 CI） | 由协作方合并为 `test/live/e2e/`（+就地 README），引用同步 7 处；`test/e2e/` 空目录已清理（18:00:18 验证为空后删除） | 2026-09-22 17:59 `--collect-only`：三层 765 不变、`test/live/e2e` 采集 10；`rg "test[/\\]e2e"` 无失效引用（仅历史说明性文字） |
| C-010 | S8 日志矩阵误红：`log_matrix_real_tb.py` 默认 `--cds-log` 指向 2026-09-21 崩溃后冻结的 `~/project/vblog/CDS.log`，而活日志是 `-log` 指定的 `/home/Gent/virtuoso_11.log` → `increment-bytes` 误报"不是连续切片" | TB 改为默认向活 CIW 询问 `hiGetLogFileName()`（`--cds-log` 仅显式覆盖），证据 JSON 记录 `cds_log/work_dir/token`；并发写入时 `window_identical` 失败会明确提示"需要独占" | 修正后：vbs11（65130/65200）全 6 例 PASS，`increment-bytes` 首次尝试即 `window_identical=true`（`test/artifacts/evidence/log-matrix-vbs11.json`）；原 `log-matrix-real-green.json` 保留为历史误红快照 |
| C-011 | S10 真机 e2e 误红：`test_e2e_live.py` 的 `_discover_current_daemon()` 会把 pgrep 列表里最老的 **fakevirt** daemon 当引导源，fake 无法 `ipcBeginProcess` → 新 daemon 起不来、verify 报 "SKILL execution timed out" | 排除 `fakevirt` 行 + 支持 `VB_E2E_BOOTSTRAP_TOKEN/PORT` 指定专用 CIW；按 `test/docs/推荐测试环境.md` §3 起专用 `vbe2e` 实例（`start_real_virtuoso.sh` + `.cdsinit` 自动 load） | 修复后 `test_e2e_live.py` **2/2 PASS**（注册 1–4 → CIW load → verify → commit → skill/上传下载/日志增量） |
| C-012 | S10 业务面真机用例误红：`test_business_remote_live.py` 自建 registry 条目只配 daemon/command/file，未配 `ssh.default`，而 `roles.resolve` 会急切解析 5 个 role → "role gui is remote but host/user is unresolved" | 补 `ssh.default.host/user` 与 gui/spectre role（同一 one-host 兜底语义） | 修复后 **4/4 PASS**（真机路由隔离、上传下载、并行命令重叠、混合并发重试） |
| C-013 | 凭据卫生（测试侧·已修，**需所有者轮换**）：内置管理员 token 的明文被硬编码进 2 个已跟踪测试文件（`test/offline/integration/test_supervisor_process.py:21`、`test/offline/unit/test_registration_server.py:24`），另 1 个 TB（`registration_http_six_step_tb.py:63`）同样硬编码 —— 与 `src/register/server.py:67`"哈希写死、原文离线保管"的设计直接冲突；`git log -S` 证实明文已进入历史提交 | ① 单测改为 monkeypatch `register_server._ADMIN_TOKEN_HASH` 为一次性测试 token（不再需要真凭据、覆盖不降）；② 集成测试与注册 TB 从 `VB_ADMIN_TOKEN` 或 gitignored `test/artifacts/admin-token.txt` 读取，缺失时显式 skip/报错（三个集成用例在无凭据时 skip 并给出原因）；③ 两个 runner 自动加载该 gitignored 文件 | 复扫 `rg` 全工作树：除 gitignored token 文件外**零命中**；单测 90 passed / 3 skipped（有 token 时 3 passed）。**遗留**：明文仍在 git 历史中，需仓库所有者轮换该 token 才能彻底消除 |
| P-039 | 缺陷（源码·已复现，结项后复核发现） | **py2.7 daemon 与 py3 daemon 对帧读取器 ValueError 的行为分歧**：py3 静默丢弃、py27 回 NACK 帧（`internal daemon error`），且到达 handler 的异常类型已被换成 `TypeError` → py27 的 `except (UnicodeDecodeError, ValueError)` 分支在这条路径上不可达、原始异常信息丢失 | `src/bridge/resources/ramic_bridge_daemon_27.py:422`（读帧）+ `:458-464`（两个 handler），对照 `_3.py:426/462-468`；探针 `test/semi/probes/daemon_internal_error_path_probe.py`：`[py3] handler saw ['ValueError'] -> sent b''` 与 `[py27] handler saw ['TypeError'] -> sent b'\x15{"error": "internal daemon error"...'` | 同一协议在两个运行时上行为不一致；畸形输入被当成"产品内部错误"回给客户端，排障方向被带偏 | **已报 bug-20260922T170201Z-vblog-9bb6c6a6**；测试侧已加特征化用例（py3 断言正确契约，py27 特征化当前行为并注明修复后必须变红） |
| P-040 | 测试基础设施（已修） | **`run_main_coverage.ps1` 缺 UTF-8 BOM**：Windows PowerShell 5.1 按 GBK 解码中文注释 → 产生伪引号 → **语法错误，报告 §6 的复算命令当时根本无法执行**；另两个 acceptance runner 用 `Out-Null` 吞掉 pytest 输出，离线层红时日志里没有任何失败信息 | 三个 runner 补 BOM 后 `Parser::ParseFile` 全部 OK；`run_main_coverage.ps1` 的离线步骤改为不吞输出，2026-09-23 复算日志中可见 `1225 passed / 2 skipped / 7 xfailed / 364 subtests` | 交付方给出的"一条命令复算"在 Windows 默认 shell 下不可用；失败不可诊断会让评审直接卡住 | 约定：仓库内 `.ps1` 一律保存为 UTF-8 with BOM；已写入 [首轮测试报告.md](首轮测试报告.md) §6.1 备注 |
### 复核期补充（2026-09-23）

| ID | 类别 | 问题 | 证据 | 影响 | 状态 |
|---|---|---|---|---|---|
| P-041 | 缺陷（源码·已复现，复核期发现） | **`verilog._read_views` 守卫长度与取值下标不匹配**：守卫写 `len(file_entry) >= 3` 却读取 `file_entry[3]` → 长度 3 的畸形条目抛 `IndexError`，而不是像其它畸形条目那样被跳过 | `src/pyapi/packages/verilog.py:272-279`；L0 复现 `test/offline/unit/test_verilog_contracts.py::TestVerilogReadCellMode::test_short_view_entry_is_rejected_as_index_error`（构造 `(("verilog" ("verilog" nil nil)))`） | 正常路径不触发（本包生成的 SKILL 恒为 4 元素），仅畸形/被截断的 SKILL 返回值命中，防御性 P3；异常被外层 try 兜成结构化失败但归因不准 | **已报 bug-20260922T161734Z-vblog-bc9ed7d9**；建议守卫改 `>= 4` 或改用容错取值 |
| P-042 | 缺陷（源码·已复现，复核期发现） | **`layout.gds` 导出遇版图锁时误报 "layout view not found"**：版图被另一会话持有（`layout.oa.cdslck`）时 `dbOpenCellViewByType(..., "a")` 返回 nil，实现只回一句 not found，调用方误判为视图不存在/名字写错 | `src/pyapi/packages/layout.py:960-975`（flush 步骤）、`:339-350`（`_view_state_expr` 已有 ok/mismatch/missing 三态判定但 flush 未用）；真机复现：同视图 `mode="r"` 可打开（`db:0x43e9641a`）、`"a"` 返回 nil，持锁实例导出成功 | 被锁场景下导出必然失败且报错误导排障方向；只读导出/已保存视图也无路可走 | **已报 bug-20260922T163705Z-vblog-83ee8603**；建议 flush 前用 `_view_state_expr` 区分 missing / mismatch / locked |

### 三级物理重组暴露（2026-09-23，均已闭环）

| ID | 类别 | 问题 | 证据 | 状态 |
|---|---|---|---|---|
| C-014 | 测试基础设施（搬迁暴露） | **两个 acceptance runner 仍按旧深度取仓库根**：`test/shared/runners/` 距根 2 层，而脚本写 3 层（`Split-Path -Parent`×3 / `..\..\..`）→ 根被算成仓库**父目录**，`Set-Location` 与套件路径全部落空，脚本实际不可运行 | `test/shared/runners/run_main_coverage.ps1:10`、`test/shared/runners/run_package_e2e.ps1:20` 修正前后对照 | **已闭环**：改为 2 层并加注释；`[Parser]::ParseFile` 两个脚本均 OK（BOM 保留） |
| C-015 | 残留引用（搬迁暴露） | ① `test/live/flows/scale_100_tb.py:35` 仍 `from test.tb.support._win import ...`（模块已不存在，靠 except 兜底运行）；② `test/semi/probes/maestro_screenshot_probe.py:9` 仍把 `test/tb` 注入 `sys.path` | 两处代码锚点；`rg "test[/\\]tb"` 复扫 | **已闭环**：①改为直接 `support/_win.py` 导入（`importlib` 导入验证 `no_window` 解析成功）；②删除无效注入（同目录 `_maestro_tb` 可直接导入）；两文件 `py_compile` 通过 |
| C-016 | 结构（第二轮重排） | **顶层仍混着 8 类非测试目录**（support/runners/standards/audit/legacy/计划/artifacts/docs 与三级并列），"环境 / 产物 / 零时产物"混在 `test/artifacts/` 同一层；仓库根散落 **49 个 `.coverage*` + 2 个日志 + 1 个空 `out`** | 重排前 `Get-ChildItem test`、`Get-ChildItem . -File`；移动映射见 `test/artifacts/tmp/_move-map.json` | **已闭环**：顶层收敛为三级 `offline/ semi/ live/` + 跨级 `shared/{fixtures,runners,standards,archive}` + `docs/`（规范）+ `reports/`（报告台账）+ `plans/`（计划）+ `artifacts/{env,evidence,tmp}`（数据）；仓库根零产物，`.gitignore` 增 `.coverage*`；两个 runner 取根深度二次修正（`test/shared/runners/` 为 3 层） |
| C-017 | 环境数据（搬迁暴露） | **registry 内嵌的绝对 local-root 仍指向 `test\tb\artifacts\...`**（`test/artifacts/env/*/registry.json`）：按该路径打开会**在旧位置重建目录树**，等于把废弃布局写回磁盘 | `test/artifacts/env/log-vblog/registry.json` 修正前后对照；`rg "test.tb.artifacts" test/artifacts/env` | **已闭环**：全部改写到 `test\artifacts\env\<name>\local-root\...`，与 `--work-dir` 新位置一致 |

### 第二轮复核（2026-09-23，真 py2.7 环境建成后）

| ID | 类别 | 问题 | 证据 | 影响 | 状态 |
|---|---|---|---|---|---|
| P-043 | 缺陷（源码·已复现） | **`ramic_bridge_daemon_27.py` 在真 Python 2.7 下直接 SyntaxError**：文件含 12 行中文注释（第 285 行起）但**没有 `# -*- coding: utf-8 -*-`**；py3 默认 UTF-8 所以一直没暴露，py2.7 默认 ASCII → daemon 根本起不来 | 真机 `python2.7 ramic_bridge_daemon_27.py` → `SyntaxError: Non-ASCII character '\xe7' ... line 286`；远程副本**仅加该 cookie** 后探针 5/5 PASS（`test/semi/probes/py27_daemon_probe.py`，证据 `test/artifacts/evidence/py27-daemon-probe.json`） | py2.7 兼容路径此前只有"py3 导入纯函数对照"= **假绿**；真解释器上 0 可用 | **待设计修**（一行）；修完我在 wsl-gent 复跑落绿证据 |
