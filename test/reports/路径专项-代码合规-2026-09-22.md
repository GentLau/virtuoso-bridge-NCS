# 路径专项审计 · 代码合规（2026-09-22）

> 审计人：测试工程师 ｜ 快照时间：**2026-09-22 18:27**（`src/transport/middle.py` 正在被编辑，行号以该时刻为准）
> 判定口径（用户裁定）：**只认代码事实**——`文件:行` + 触发条件 + 该代码算出的路径；推荐路径当时可用却没被使用才算不合规。
> **磁盘上有没有残留文件不构成证据**（可能来自旧版本、一次性脚本、重构前代码）；盘干净也不等于合规。
> 相关的现场清点数据在另一份报告 [路径专项-2026-09-22.md](路径专项-2026-09-22.md)，其中"实测"部分一律只是现场迹象。

## 1. 方法与覆盖率

扫描范围：`src/**` 与 `test/**` 的 `.py` / `.il` / `.ps1`（排除 `**/__pycache__/**`、`test/artifacts/**`）。

| 检索面 | 模式（rg） | 命中 |
|---|---|---|
| 临时目录/文件 | `mkdtemp\|NamedTemporaryFile\|TemporaryDirectory\|gettempdir` | src 8 处、test 136+ 处（见 §2） |
| 系统 temp 变量 | `TMPDIR\|TEMP\|TMP`、`tempfile.gettempdir()` | src 2 处（`paths.py:38`、`ssh.py:237`） |
| 家目录 | `Path.home()`、`expanduser` | src 10 处（均为**本地**语义，见 §3）、test 少量 |
| `/tmp` 字面量 | `/tmp/\|"/tmp"\|/var/tmp` | src 1 处（`ssh.py:237`）；test 多为纯字符串夹具（§3 反例） |
| `~` / `$HOME` | `~/\|\$HOME` | src 6 类：**均为"解析成远端 HOME"的正确写法**（§3） |
| cwd 依赖 | `getcwd\|chdir\|os.curdir` | **零命中**（src 与 test） |
| 硬编码盘符 | `C:\\` | **零命中**（`doc/` 与 `test/artifacts/` 除外，未纳入） |

## 2. 不合规清单（核心）

| 编号 | 归属 | 位置 | 代码事实 | 推荐路径（当时可用） | 触发条件 | 建议 | 级别 |
|---|---|---|---|---|---|---|---|
| A-1 | src（客户端） | `src/transport/middle.py:204` | `self._err_dir = Path(tempfile.mkdtemp(prefix="vb_local_err_"))` | 注入的 work root（同文件 `:202` 已支持 `dir=err_dir`；生产调用点 `:685` 传 `err_dir=temp_dir()`） | 该 runner 被**不带 `err_dir`** 构造时（测试/未来调用方） | `else` 分支回退到 `common.paths.temp_dir()`，或直接要求必填 | P3（当前生产路径不触发，属潜在分支） |
| A-2 | src（客户端） | `src/common/ssh.py:586-591` | 隧道 stderr 重定向到临时日志：`NamedTemporaryFile(prefix="vb_tunnel_stderr_", suffix=".log", delete=False)` **无 `dir=`** → 落系统 temp；**失败路径**会读它并 `os.unlink`（`:644`），但**成功路径** `return proc`（`:620-621`）与 `return None`（`:625-626`）都不删，且路径未写入隧道状态（`_note_tunnel_ready:453-466` 只记 port/pid/proc）→ `stop_port_forward`（`:721`）也无从删除 | work root 的 `temp/`（`common.paths.temp_dir()`）；或把路径登记进 runner 状态、在 teardown 删除 | 每次**成功**建立 ssh 隧道 | 二选一：①落到 work root；②`_note_tunnel_ready` 记录 stderr 路径，`stop_port_forward`/teardown 删除（隧道存活期间子进程持有句柄，不能立即删） | P2 |
| A-3 | test（L0–L2） | `test/conftest.py:20` + **23 个文件 / 76 处**真漏点 | 清理 fixture 只匹配 `vb-*`；`tempfile.mkdtemp()` 无 prefix 时生成 `tmpXXXX`，永不被清理（另有 45 处位于已带 `rmtree`/`tearDown` 的文件，不算漏点） | 统一 `prefix="vb-"`（现有清理机制）或 `TemporaryDirectory` | 每次跑测 | **已修**：121 处加 `prefix="vb-"`；conftest 改 **session 级**回收（§6） | ~~P2~~ 已闭环 |
| A-4 | test（L1） | `test/offline/integration/test_supervisor_process.py:47-50`、`test/offline/integration/test_business_child_supervision.py:26-29` | `mkdtemp(prefix="vb-supervisor-", dir=artifact_root)`，`artifact_root = ROOT/"test"/"tb"/"artifacts"` | 系统 temp（前缀 `vb-`，交给 conftest） | 每次跑这两个用例 | **已修**：去掉 `dir=artifact_root`；18:36–18:39 全量跑测未再新增证据树残留 | ~~P2~~ 已闭环 |
| A-5 | test（TB） | ~~`test/offline/core/api_server_tb.py:139`、`semantics_tb.py:61`、`fault_injection_tb.py:46`~~ | **本条撤回（18:35 更正）**：三个脚本都已自清理——`semantics_tb.py:564`、`fault_injection_tb.py:435` 调 `cleanup_temp_dirs()`；`api_server_tb.py:491` 在 `finally` 里 `shutil.rmtree(work_dir)` | — | — | 无需修复（**合规**） | ~~P2~~ 撤回 |
| A-6 | test（TB 真机） | `test/live/e2e/test_e2e_live.py:109`、`test_business_remote_live.py:88,214,233`、`test_business_local_live.py:55,222,238` | 裸 `mkdtemp()`（含 `override_work_dir_for_tests` 用） | `--work-dir` 语义 / `vb-` 前缀 | 跑真机 pytest | **已修**：统一 `prefix="vb-"`；pytest 入口下由 `test/conftest.py` 会话级回收 | ~~P3~~ 已闭环 |
| A-7 | test（legacy，非准出） | `test/shared/archive/*`（14 处 `mkdtemp()`）+ `p2_extreme_gradient.py:41` 等硬编码 `/root/.virtuoso-bridge/...` | 历史脚本 | role root / `--root` | 仅手工复跑 | 不修；如需复活先按规范重写 | P3 |

## 3. 合规清单（反例，避免重复怀疑）

| 位置 | 事实 |
|---|---|
| `src/server/stress_server.py:47,164` | `TemporaryDirectory(dir=root, prefix="stage-")` —— 用了注入 root |
| `src/register/probe.py:71-88` | `NamedTemporaryFile(delete=False)` 但 `finally: tmp_path.unlink()` —— 有清理 |
| `src/transport/middle.py:685` | 生产调用点显式传 `err_dir=temp_dir()`（work root 之下） |
| `src/transport/tunnel.py:45,191,207`、`src/register/flow.py:210-216`、`src/common/remote_paths.py:19` | `~` 是**被解析**为远端 `$HOME`（`printf "$HOME"`），不是造字面量 `~` 目录 |
| `src/common/paramiko_backend.py:331,377` | `Path.home()` 用于**本地** known_hosts / ssh_config `%d` 展开，符合语义 |
| `test/offline/core/daemon_log_protocol_tb.py:77,114,155,176,216` | `with tempfile.TemporaryDirectory()` —— 上下文自动清理 |
| `test/**` 里大量 `Path("/tmp/x")` | 纯字符串夹具（路径拼接测试），不触发任何写盘 |

## 4. 无法判定 / 设计内例外

| 项 | 说明 |
|---|---|
| `src/common/ssh.py:237` | ControlMaster socket 落系统 temp（OpenSSH AF_UNIX 路径长度约束）——**设计内例外**；缺"崩溃后清理"，建议单独登记 |
| bridge 部署 `/tmp/virtuoso_bridge_<remote_user>/<client_id>/` | `AGENTS.md` 描述的设计形态，与 [文件使用规范.md](../docs/文件使用规范.md)"禁共享 `/tmp`"冲突——**口径未定稿**（P-011），不下结论 |
| `/home/Gent/~`、`homeGent.virtuoso-bridgee2e-*` | 现场存在，但**未定位到产生它们的代码**；按本轮口径记 PENDING，不得当违规引用（P-010） |

## 5. 现场迹象（非判定依据）

`%TEMP%` 与本机家目录的 vb 前缀条目、wsl-gent `/tmp` 的条目统计见 [路径专项-2026-09-22.md](路径专项-2026-09-22.md) §3 D1–D4。
它们只在 §2 的代码成立时才具有解释力，本身不作为任何结论的证据。

## 6. 修订记录（同日）

| 时间 | 修订 | 依据 |
|---|---|---|
| 18:35 | **A-5 撤回**：3 个 `tb/core` 脚本已自清理，误判源于只看了 `mkdtemp` 调用点、没看收尾函数 | `semantics_tb.py:564`、`fault_injection_tb.py:435`、`api_server_tb.py:491` |
| 18:40 | **A-3 数字修正**：真漏点是 23 个文件 / 76 处（此前把"文件里出现 bare `mkdtemp`"一律当漏点，未区分同文件已有 `rmtree`/`tearDown` 的情况） | 分类脚本（已有清理机制的文件从漏点集合剔除） |
| 18:45 | **A-3 / A-4 / A-6 修复落地**（test 侧由测试工程师直接修，不走 bug API） | 三层全量 **794 passed / 2 skipped**；隔离 TEMP 复跑 `leftover_vb_dirs=0`；18:36–18:39 跑测未在 `test/artifacts/` 新增残留 |
