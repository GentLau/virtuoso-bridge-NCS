# 拓扑与传输后端

> 覆盖目标：配置一览 §2/§6.5、多节点 §6；环境四节点全组合
> 判定粒度：每个测试项都将由对应 TB 完整执行一次；本文件只定"测什么、怎么测、通过标准是什么"，具体代码归 `test/` 与 `test/`。

### 1. 四节点冒烟

- **测试目标**：本机/glis/wsl/vps 全组合
- **测试流程**：每组合 skill+命令+文件往返
- **预计响应**：全部通过

### 2. 显式 jump vs config

- **测试目标**：等价且显式优先
- **测试流程**：同一目标两路由
- **预计响应**：结果一致；显式覆盖 config

### 3. proxy SOCKS5

- **测试目标**：经代理可达
- **测试流程**：proxy 配置调用
- **预计响应**：可达且 endpoint key 含 proxy 原文

### 4. Windows local role

- **测试目标**：Windows 上 local 语义
- **测试流程**：glis/本机 local 命令+文件
- **预计响应**：与 WSL local 一致

### 5. backend 切换

- **测试目标**：openssh/paramiko 结果一致
- **测试流程**：双后端同序列
- **预计响应**：结果集一致

### 6. max_sessions

- **测试目标**：配置在 role、生效在 endpoint，多 role 同 endpoint 取最小值
- **测试流程**：并发=max_sessions 与 max_sessions+1；共享 endpoint 的两个 role 配置不同值
- **预计响应**：上限内成功；超限 `role max_sessions exceeded`；有效上限取最小值

### 7. 端口 22 校验

- **测试目标**：非 22 注册拒绝
- **测试流程**：alias 配置 Port 2222 后注册
- **预计响应**：注册 ERROR，不静默接受
